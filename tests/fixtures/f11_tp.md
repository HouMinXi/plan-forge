## Implementation

This plan adds the duplicate-phrase detection logic to the gate framework.

<acceptance_criteria>
Run test_nonexistent_function_xyz to verify the gate fires on the fixture.
The test must be present in the test suite and must pass without error.
</acceptance_criteria>
