## Implementation

This plan delivers the temporal anchor lint gate with anchored criteria.

<acceptance_criteria>
after `uv run pytest tests/unit/ -q` passes all tests, merge the branch
into main and close the milestone tracker entry.
</acceptance_criteria>
