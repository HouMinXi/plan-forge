# P1 Fail Fixture

This plan has an orphan identifier with no cross-reference.

## Overview

The `exotic_widget` module handles rendering.  It is only mentioned here.

The `parse_plan` function appears at least twice: `parse_plan` is
the main entry point used throughout.

## Design

Use `exotic_widget` - wait, this line also mentions it.

Actually the line above already counts as the second mention.
We need a truly orphan identifier below.

The `singleton_factory` is introduced here exactly once with no
cross-reference, no marker, and no second mention anywhere.
