---
phase: 11-f3-per-mention-dedup-and-own-id-suppression
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py
autonomous: false
requirements:
  - F3-01
  - F3-02

must_haves:
  truths:
    - "test_f3_dedup_fires_once PASSES (GREEN: Phase 8 x6 produces exactly 1 finding)"
    - "test_f3_own_id_suppressed_via_frontmatter PASSES (GREEN: 08-02 from frontmatter exempted)"
    - "test_f3_fail_fixture PASSES (genuine TP still fires)"
    - "test_f3_self_ref_silent PASSES (heading-based own-ID still works)"
    - "test_f3_cross_ref_still_fires PASSES (02-03 cross-ref still detected)"
    - "All 8 pre-existing F3 tests still pass (no regression)"
    - "Bug-inject cycle GATE-11T completed: reverting dedup key causes 6 findings; restoring causes 1; TP unaffected"
    - "Full test suite green (766+ baseline + 2 new tests, minus 1 pre-existing version failure)"
  artifacts:
    - path: "src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py"
      provides: "F3 check with per-claim dedup (seen key = claim.lower()) and frontmatter+heading union own-ID extraction"
      contains: "_BOM"
  key_links:
    - from: "f3_cross_plan_invariant.py _own_id_set"
      to: "parsed.raw_text"
      via: "splitlines() frontmatter scan before heading scan"
      pattern: "_BOM"
    - from: "f3_cross_plan_invariant.py check() seen set"
      to: "claim.lower()"
      via: "dedup key is normalized claim text, not (claim, lineno) tuple"
      pattern: "claim.lower()"
---

<objective>
Apply both fixes to f3_cross_plan_invariant.py: change the seen-set dedup key
from (claim, lineno) to claim.lower() (per D-01), and extend _own_id_set with
YAML frontmatter scanning using the verified reference implementation from
CONTEXT.md (per D-03/D-04). Run the bug-inject cycle (GATE-11T) before
committing.

Purpose: F3 currently produces duplicate findings when the same cross-plan
reference appears on multiple lines (e.g. "Phase 8" x6 = 6 HIGH findings
instead of 1). It also flags a plan's own ID as an unverified cross-plan ref
when the plan uses GSD YAML frontmatter instead of ATX headings. Both are
false positives that erode signal-to-noise ratio.

Output: Modified f3_cross_plan_invariant.py with GREEN tests for F3-01 and
F3-02; bug-inject teeth verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/houminxi/code/plan-forge/.planning/phases/11-f3-per-mention-dedup-and-own-id-suppression/11-CONTEXT.md
@/home/houminxi/code/plan-forge/.planning/phases/11-f3-per-mention-dedup-and-own-id-suppression/11-RESEARCH.md
@/home/houminxi/code/plan-forge/.planning/phases/11-f3-per-mention-dedup-and-own-id-suppression/11-01-SUMMARY.md

<interfaces>
From src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py (current, 128 lines):

  Line 14: import re
  Line 24: _SUBPLAN_RE = re.compile(r'(?<!\d-)\b0[1-9]-\d{2}\b')
  Lines 28-34: _EXEMPT_KEYWORDS tuple

  Lines 46-65: _own_id_set(parsed) -- REPLACE body with reference implementation
  from CONTEXT.md specifics section. Function signature unchanged.

  Lines 78-127: check(parsed, **kwargs):
    Line 104: seen: set[tuple[str, int]] = set()   -> CHANGE TO: seen: set[str] = set()
    Line 109: key = (claim, lineno)                 -> CHANGE TO: key = claim.lower()
    Lines 110-112: if key in seen / continue / seen.add(key) -- logic unchanged

Key constraint: the _BOM module-level constant must be added near other
module-level constants (after _EXEMPT_KEYWORDS, before _heading_is_exempt).

Reference implementation for _own_id_set (from CONTEXT.md, verified correct
by 4 rounds of cross-AI review -- use verbatim):

  _BOM = chr(0xFEFF)  # U+FEFF; not removed by str.strip()

  def _own_id_set(parsed: ParsedPlan) -> set[str]:
      own: set[str] = set()
      lines = parsed.raw_text.splitlines()
      if not lines:
          return own

      # Frontmatter: opening --- must be on line 1 (or line 2 if line 1 empty)
      first = lines[0].lstrip(_BOM).strip()
      second = lines[1].lstrip(_BOM).strip() if len(lines) > 1 else ""
      fm_start = None
      if first == "---":
          fm_start = 1
      elif first == "" and second == "---":
          fm_start = 2

      if fm_start is not None:
          phase_num = plan_num = None
          for line in lines[fm_start:]:
              stripped = line.strip()
              if stripped.startswith("#") or stripped == "---":
                  break
              if m := re.match(r'phase:\s*["\']?(\d+)', stripped):
                  phase_num = m.group(1).zfill(2)
              if m := re.match(r'plan:\s*["\']?(\d+)', stripped):
                  plan_num = m.group(1).zfill(2)
          if phase_num and plan_num:
              own.add(f"{phase_num}-{plan_num}")

      # Heading scan (always runs -- union with frontmatter result)
      for line in lines:
          if line.strip().startswith("#"):
              heading = line.strip().lstrip("#").strip()
              for m in _SUBPLAN_RE.finditer(heading):
                  own.add(m.group(0).lower())
              break  # first heading only

      return own
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply dedup key change and frontmatter own-ID extension</name>
  <files>src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py</files>

  <read_first>
    - /home/houminxi/code/plan-forge/src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py
      (full file, 128 lines; edit targets: _BOM constant, _own_id_set body,
      check() seen-set type and key)
    - /home/houminxi/code/plan-forge/tests/fixtures/f3_fail.md
      (existing TP fixture; confirm it still fires after changes)
    - /home/houminxi/code/plan-forge/.planning/phases/11-f3-per-mention-dedup-and-own-id-suppression/11-01-SUMMARY.md
      (confirm Plan 01 baseline: exact test count and RED state)
  </read_first>

  <behavior>
    After the edit:
    - test_f3_dedup_fires_once: PASSES (seen key is claim.lower(); "phase 8"
      is one key; first occurrence fires, 5 subsequent skipped; exactly 1
      finding)
    - test_f3_own_id_suppressed_via_frontmatter: PASSES (frontmatter scan
      extracts phase "08" + plan "02" -> own.add("08-02"); body "08-02"
      matches _SUBPLAN_RE but is in verified set; zero findings for 08-02)
    - test_f3_self_ref_silent: PASSES (f3_self_ref.md starts with # heading,
      fm_start is None, heading scan extracts 02-01 as before)
    - test_f3_cross_ref_still_fires: PASSES (02-03 is not in own set and
      not in any audit section; still fires)
    - test_f3_fail_fixture: PASSES (TP claims not in verified set; still fires)
    - test_f3_date_fragment_silent: PASSES (date lookbehind unchanged)
    - test_f3_pass_fixture: PASSES (all refs covered in References section)
    - test_f3_severity: PASSES (severity unchanged)
    - test_f3_edge_case_insensitive_exemption: PASSES (case handling unchanged)
    - test_f3_edge_audit_notes_exemption: PASSES (audit section handling unchanged)
  </behavior>

  <acceptance_criteria>
    - f3_cross_plan_invariant.py contains _BOM = chr(0xFEFF) as module-level
      constant after _EXEMPT_KEYWORDS
    - _own_id_set body matches the reference implementation from CONTEXT.md
      verbatim (frontmatter scan before heading scan, union result)
    - _own_id_set docstring updated to describe frontmatter + heading behavior
    - check() line 104: seen: set[str] = set() (not set[tuple[str, int]])
    - check() line 109: key = claim.lower() (not (claim, lineno))
    - Code comment about Phase 10+ own-ID limitation near _BOM or in
      _own_id_set docstring, written as a human engineering note (no
      plan/task labels)
    - All 10 F3 tests pass (8 existing + 2 new from Plan 11-01)
    - Step 0 clean on f3_cross_plan_invariant.py diff
  </acceptance_criteria>

  <action>
    Edit inside .worktrees/f3-per-mention-dedup/ only.

    PART A -- Add _BOM constant after the _EXEMPT_KEYWORDS tuple (after
    line 34, before the _heading_is_exempt function):

      _BOM = chr(0xFEFF)  # U+FEFF; not removed by str.strip()

    PART B -- Replace the body of _own_id_set (lines 46-65) with the
    reference implementation from CONTEXT.md specifics section. Copy it
    verbatim. Update the docstring to reflect the new behavior:

      Scans YAML frontmatter for phase:/plan: fields, then scans the
      first ATX heading for subplan IDs. Results are unioned -- both
      sources contribute to the own-ID set. Frontmatter detection
      strips U+FEFF BOM before the --- check.

    Add a code comment about the Phase 10+ limitation. Place it near the
    _BOM constant or in the _own_id_set docstring. Write it as a human
    engineering note:

      Phase 10+ IDs (two-digit phase numbers like 10-02) are not matched
      by _SUBPLAN_RE in the document body, so frontmatter extraction adds
      them to the verified set but suppression has no practical effect.
      Extending the subplan pattern is deferred.

    PART C -- In check(), change the seen-set key:

      Before (line 104): seen: set[tuple[str, int]] = set()
      After:             seen: set[str] = set()

      Before (line 109): key = (claim, lineno)
      After:             key = claim.lower()

    Lines 110-112 (if key in seen / continue / seen.add(key)) stay as-is.
    The location=f"line:{lineno}" on line 116 stays as-is -- because dedup
    skips later occurrences, lineno is always the first occurrence's line
    number (per D-02).

    Also update the check() docstring (line 85): change "one per unverified
    claim per line" to "one per unique unverified claim" to reflect the new
    dedup behavior.

    Step 0 pre-check on modified source file:
      python3 -m py_compile
        src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py
      ruff check src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -P '[^\x00-\x7F]' && echo "FAIL: non-ASCII" || echo "OK"
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]|P[0-9]+:|S[0-9]+:' &&
        echo "FAIL: AI smell" || echo "OK"

    Run F3 tests to confirm GREEN:
      uv run pytest tests/unit/test_f3_cross_plan_invariant.py -q
    Expected: 10 passed, 0 failed.
  </action>

  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/f3-per-mention-dedup && uv run pytest
      tests/unit/test_f3_cross_plan_invariant.py -q 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    All 10 F3 tests pass including test_f3_dedup_fires_once (1 finding),
    test_f3_own_id_suppressed_via_frontmatter (0 findings for 08-02),
    test_f3_self_ref_silent (heading-based own-ID preserved),
    test_f3_cross_ref_still_fires (02-03 genuine cross-ref still fires),
    and test_f3_fail_fixture (TP still fires). Step 0 clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bug-inject cycle GATE-11T (teeth verification)</name>

  <what-built>
    F3 source fix is GREEN. Before committing, the implementer must perform
    the bug-inject cycle to confirm the dedup guard is the operative
    suppression -- not that F3 is inadvertently disabled for all inputs.
  </what-built>

  <how-to-verify>
    Perform the following steps from inside .worktrees/f3-per-mention-dedup/:

    STEP A -- Revert dedup key (working-tree only, no commit):
    In f3_cross_plan_invariant.py, temporarily change the seen-set key
    back to the pre-fix form:

      Change: seen: set[str] = set()
      Back to: seen: set[tuple[str, int]] = set()

      Change: key = claim.lower()
      Back to: key = (claim, lineno)

    Do NOT touch _own_id_set -- only revert the dedup key to isolate
    the dedup guard.

    STEP B -- Run test_f3_dedup_fires_once with injected bug:
      uv run pytest
        tests/unit/test_f3_cross_plan_invariant.py::test_f3_dedup_fires_once
        -xvs

    Expected: FAILED (AssertionError: 6 findings, not 1)
    This confirms the dedup FP fixture fires 6 findings when the dedup
    guard is removed.

    STEP C -- Restore the fix:
      git checkout -- src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py

    STEP D -- Confirm the fix is back and tests pass:
      uv run pytest
        tests/unit/test_f3_cross_plan_invariant.py::test_f3_dedup_fires_once
        -xvs
    Expected: PASSED (exactly 1 finding)

      uv run pytest
        tests/unit/test_f3_cross_plan_invariant.py::test_f3_own_id_suppressed_via_frontmatter
        -xvs
    Expected: PASSED (zero 08-02 findings)

    STEP E -- Confirm the TP still fires after restore:
      uv run pytest
        tests/unit/test_f3_cross_plan_invariant.py::test_f3_fail_fixture
        -xvs
    Expected: PASSED (genuine cross-plan ref still fires F3 findings)

    Record the result for the commit message:
      Fixture: f3_dedup_fp.md
      Before restore (dedup key reverted): 6 F3 findings for Phase 8
      After restore: 1 finding (deduplicated)
      TP (f3_fail.md): still fires after restore (unaffected)
  </how-to-verify>

  <resume-signal>
    Type "gate11t-confirmed: dedup FP fires 6 when key reverted, 1 when
    restored, TP unaffected" to confirm bug-inject cycle passed, then
    proceed to commit.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Full suite green and commit F3 fix</name>
  <files>src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py</files>

  <read_first>
    - Result of Task 2 (bug-inject cycle outcome -- finding count before/after)
    - git -C /home/houminxi/code/plan-forge log --oneline -3 (commit style ref)
  </read_first>

  <acceptance_criteria>
    - Full test suite passes (766+ baseline + 2 new F3 tests, minus 1
      pre-existing test_version_is_alpha2 failure which is unrelated)
    - Commit on fix/f3-dedup-v013 with human-voice message
    - Commit message body includes bug-inject outcome in human-voice prose
    - No AI smell in commit (no task IDs, no model names, no Co-Authored-By)
    - Signed-off-by: Minxi Hou <houminxi@gmail.com>
  </acceptance_criteria>

  <action>
    From inside .worktrees/f3-per-mention-dedup/, run full suite:
      uv run pytest tests/ -q

    Must pass 766+ baseline tests plus the 2 new F3 tests (total 768+).
    1 pre-existing failure (test_version_is_alpha2) is expected and unrelated.
    If any other pre-existing test fails that was passing before this plan,
    stop and diagnose.

    Also run the integration tests explicitly to confirm snapshot stability:
      uv run pytest tests/integration/test_api_mechanical_e2e.py -q

    If green, stage and commit with a human-voice message. The message body
    records what the bug-inject cycle showed. No task IDs, no model names,
    no review metadata, no Co-Authored-By AI lines. Use this structure:

      checks/f3: deduplicate per-claim findings and extract own-ID from frontmatter

      F3 reported one finding per line occurrence of the same cross-plan
      reference. A plan mentioning "Phase 8" six times produced six HIGH
      findings for the same claim. Change the dedup key from (claim, lineno)
      to the normalized claim text so only the first occurrence fires.

      GSD plans use YAML frontmatter instead of ATX headings. The own-ID
      extractor only scanned headings, so GSD plans flagged their own plan
      ID as an unverified cross-plan reference. Extend _own_id_set to scan
      frontmatter for phase:/plan: fields (with BOM handling) and union the
      result with the heading scan.

      Bug-inject confirmed: reverting the dedup key causes f3_dedup_fp.md
      to produce 6 findings instead of 1. Restoring brings it back to 1.
      TP fixture f3_fail.md continues to fire.

      Signed-off-by: Minxi Hou <houminxi@gmail.com>

    Stage the source file and the F3-related test artifacts created in Plan 01:
      git add src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py
        tests/fixtures/f3_dedup_fp.md
        tests/fixtures/f3_own_id_fp.md
        tests/unit/test_f3_cross_plan_invariant.py

    Note: this commit is on the feature branch fix/f3-dedup-v013. It is a
    pre-review commit. Plan 11-03 runs the full forge review pipeline; if the
    review sub-session produces findings, amendment commits may be added to
    this branch before merge.
  </action>

  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/f3-per-mention-dedup &&
      uv run pytest tests/ -q 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    Full suite green. F3 fix committed on fix/f3-dedup-v013. Commit message
    is human-voice with bug-inject result recorded. No AI smell in commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed.raw_text -> frontmatter scan | Internal Python; raw_text from parser, no user input at runtime |
| YAML frontmatter fields -> own-ID set | Regex extraction of phase:/plan: values; malformed values produce no ID (safe failure) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | f3_cross_plan_invariant.py dedup logic | mitigate | bug-inject cycle (GATE-11T) confirms dedup key is load-bearing; TP test confirms over-suppression is not occurring |
| T-11-02 | Spoofing | crafted frontmatter suppresses legitimate cross-plan ref | accept | frontmatter only suppresses the plan's OWN id; an attacker controlling plan content could suppress one id but this is the plan author; no privilege escalation |
| T-11-03 | Denial of Service | malformed frontmatter causes infinite loop | mitigate | loop breaks on closing --- or first # heading; finite iteration over splitlines() |
| T-11-SC | Tampering | no new packages installed | accept | no package manager installs; legitimacy gate not applicable |
</threat_model>

<verification>
From inside .worktrees/f3-per-mention-dedup/:

  uv run pytest tests/unit/test_f3_cross_plan_invariant.py -v

All tests pass including:
  test_f3_dedup_fires_once -- PASSED (exactly 1 finding for Phase 8 x6)
  test_f3_own_id_suppressed_via_frontmatter -- PASSED (08-02 from frontmatter exempted)
  test_f3_self_ref_silent -- PASSED (heading-based own-ID still works)
  test_f3_cross_ref_still_fires -- PASSED (02-03 genuine cross-ref fires)
  test_f3_fail_fixture -- PASSED (TP still fires)
  test_f3_pass_fixture -- PASSED
  test_f3_severity -- PASSED
  test_f3_edge_case_insensitive_exemption -- PASSED
  test_f3_edge_audit_notes_exemption -- PASSED
  test_f3_date_fragment_silent -- PASSED
</verification>

<success_criteria>
1. f3_cross_plan_invariant.py contains _BOM = chr(0xFEFF) module-level constant
2. _own_id_set matches CONTEXT.md reference implementation (frontmatter + heading union)
3. check() seen key is claim.lower(), type annotation is set[str]
4. Code comment about Phase 10+ limitation in human-voice prose
5. All 10 F3 tests pass (8 existing + 2 new)
6. Full suite: 766+ pass, 0 new failures (1 pre-existing version test excluded)
7. Integration snapshot tests stable (F3 count = 1 unchanged)
8. Bug-inject cycle GATE-11T confirmed (6 findings when key reverted; 1 when
   restored; TP unaffected)
9. Commit on fix/f3-dedup-v013 with human-voice message and Signed-off-by
10. Step 0 clean on f3_cross_plan_invariant.py diff
</success_criteria>

<output>
Create .planning/phases/11-f3-per-mention-dedup-and-own-id-suppression/11-02-SUMMARY.md when done.
</output>
