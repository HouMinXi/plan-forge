---
phase: 11-f3-per-mention-dedup-and-own-id-suppression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .worktrees/f3-per-mention-dedup (worktree created)
  - tests/fixtures/f3_dedup_fp.md
  - tests/fixtures/f3_own_id_fp.md
  - tests/unit/test_f3_cross_plan_invariant.py
autonomous: true
requirements:
  - F3-01
  - F3-02

must_haves:
  truths:
    - "test_f3_dedup_fires_once FAILS against unmodified source (RED: 6 findings, not 1)"
    - "test_f3_own_id_suppressed_via_frontmatter FAILS against unmodified source (RED: 08-02 finding not suppressed)"
    - "All 8 pre-existing F3 tests still pass (regression guard active)"
    - "Two new fixture .md files exist with parser-valid markdown"
    - "Worktree at .worktrees/f3-per-mention-dedup/ is active; main worktree is untouched"
  artifacts:
    - path: ".worktrees/f3-per-mention-dedup"
      provides: "linked worktree for all Phase 11 code changes"
    - path: "tests/fixtures/f3_dedup_fp.md"
      provides: "plan referencing Phase 8 six times with no audit section; pre-fix: 6 findings, post-fix: 1"
    - path: "tests/fixtures/f3_own_id_fp.md"
      provides: "GSD PLAN.md with phase:08, plan:02 in YAML frontmatter and 08-02 in body; pre-fix: 1 finding, post-fix: 0"
    - path: "tests/unit/test_f3_cross_plan_invariant.py"
      provides: "two new test functions: test_f3_dedup_fires_once, test_f3_own_id_suppressed_via_frontmatter"
  key_links:
    - from: "tests/unit/test_f3_cross_plan_invariant.py"
      to: "tests/fixtures/f3_dedup_fp.md"
      via: "_load() in test_f3_dedup_fires_once"
      pattern: "f3_dedup_fp"
    - from: "tests/unit/test_f3_cross_plan_invariant.py"
      to: "tests/fixtures/f3_own_id_fp.md"
      via: "_load() in test_f3_own_id_suppressed_via_frontmatter"
      pattern: "f3_own_id_fp"
---

<objective>
Create the worktree and write all Wave 0 test scaffolding -- two new fixture
files and two new test functions -- before any source code is touched.

Purpose: Establish the RED state that makes the fix measurable. Each new test
must fail against the unmodified source, proving the fixtures and assertions
are load-bearing. The 8 existing F3 tests serve as the regression guard.

Output: Active worktree at .worktrees/f3-per-mention-dedup/, two fixture .md
files, and two new test functions in RED state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/houminxi/code/plan-forge/.planning/ROADMAP.md
@/home/houminxi/code/plan-forge/.planning/phases/11-f3-per-mention-dedup-and-own-id-suppression/11-CONTEXT.md
@/home/houminxi/code/plan-forge/.planning/phases/11-f3-per-mention-dedup-and-own-id-suppression/11-RESEARCH.md

<interfaces>
From src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py (current, 128 lines):

  Line 23: _PHASE_RE = re.compile(r'\bphase[-\s]\d+\b', re.IGNORECASE)
  Line 24: _SUBPLAN_RE = re.compile(r'(?<!\d-)\b0[1-9]-\d{2}\b')
  Line 25: _TASK_RE = re.compile(r'\bT\d{2,3}\b')

  Lines 46-65: _own_id_set(parsed) -- scans first ATX heading only; returns
  empty set for GSD PLAN.md files (no heading before body content).

  Lines 78-127: check(parsed, **kwargs) -- seen: set[tuple[str, int]] with
  key = (claim, lineno). Current behavior: same claim on different lines
  produces separate findings.

  Line 104: seen: set[tuple[str, int]] = set()
  Line 109: key = (claim, lineno)

Key facts for fixture design:
  - _PHASE_RE matches "phase 8", "Phase 8", "Phase-8" (case insensitive).
  - _SUBPLAN_RE matches "08-02" but NOT "10-02" (requires first digit = 0).
  - Fixture f3_dedup_fp.md must reference "Phase 8" on 6 different lines
    with NO audit/references section -> 6 findings pre-fix, 1 post-fix.
  - Fixture f3_own_id_fp.md must have YAML frontmatter with phase: "08"
    and plan: "02", and body text containing "08-02" -> 1 finding pre-fix
    (own-ID not extracted from frontmatter), 0 post-fix.

From tests/unit/test_f3_cross_plan_invariant.py (8 existing tests):
  _FIXTURES = pathlib.Path(__file__).parent.parent / "fixtures"
  def _load(name: str) -> str:
      return (_FIXTURES / name).read_text(encoding="utf-8")

From src/plan_forge/parser.py line 70:
  ParsedPlan.raw_text: str  # full plan text
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create worktree and verify baseline</name>
  <files>.worktrees/f3-per-mention-dedup (worktree only; no main-tree files changed)</files>

  <read_first>
    - /home/houminxi/code/plan-forge/.gitignore (verify .worktrees/ entry exists)
    - Result of: git -C /home/houminxi/code/plan-forge worktree list
    - Result of: cd /home/houminxi/code/plan-forge && uv run pytest
        tests/unit/test_f3_cross_plan_invariant.py -q
      (baseline: 8 passed expected per RESEARCH.md)
  </read_first>

  <acceptance_criteria>
    - git worktree list shows .worktrees/f3-per-mention-dedup at fix/f3-dedup-v013
    - Baseline F3 test count is 8 passed
    - Main worktree git status is clean
  </acceptance_criteria>

  <action>
    From /home/houminxi/code/plan-forge (the main repo root), run:

      git worktree add .worktrees/f3-per-mention-dedup -b fix/f3-dedup-v013

    This creates a linked worktree on a new branch fix/f3-dedup-v013.
    All file edits in Plans 11-01, 11-02, and 11-03 happen inside
    .worktrees/f3-per-mention-dedup/, never in the main worktree.

    Verify .worktrees/ is in .gitignore. If absent, append it.

    From inside .worktrees/f3-per-mention-dedup/, run:
      uv run pytest tests/unit/test_f3_cross_plan_invariant.py -q

    Record the exact count. Expected: 8 passed. This is the regression floor.
  </action>

  <verify>
    <automated>git -C /home/houminxi/code/plan-forge worktree list | grep f3-per-mention-dedup</automated>
  </verify>

  <done>
    Worktree .worktrees/f3-per-mention-dedup/ exists at branch fix/f3-dedup-v013.
    Baseline: 8 F3 tests passed. Main worktree is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixture files and failing test stubs (RED)</name>
  <files>
    tests/fixtures/f3_dedup_fp.md,
    tests/fixtures/f3_own_id_fp.md,
    tests/unit/test_f3_cross_plan_invariant.py
  </files>

  <read_first>
    - /home/houminxi/code/plan-forge/tests/fixtures/f3_fail.md
      (existing TP fixture; confirm its structure for reference)
    - /home/houminxi/code/plan-forge/tests/fixtures/f3_self_ref.md
      (heading-based own-id fixture; pattern reference)
    - /home/houminxi/code/plan-forge/src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py
      (full file, 128 lines; understand _PHASE_RE capture, _own_id_set heading-only
      scan, and check() seen-key behavior)
    - /home/houminxi/code/plan-forge/tests/unit/test_f3_cross_plan_invariant.py
      (full file, 139 lines; add new functions after test_f3_cross_ref_still_fires)
  </read_first>

  <behavior>
    - test_f3_dedup_fires_once: asserts exactly 1 F3 finding for "Phase 8".
      Against unmodified source (seen key is (claim,lineno)), "Phase 8" on 6
      different lines produces 6 findings, not 1. Test FAILS. RED gate for F3-01.
    - test_f3_own_id_suppressed_via_frontmatter: asserts zero F3 findings
      containing "08-02". Against unmodified source (_own_id_set scans headings
      only; GSD frontmatter yields empty own set), the 08-02 body reference
      fires F3. Test FAILS. RED gate for F3-02.
  </behavior>

  <acceptance_criteria>
    - tests/fixtures/f3_dedup_fp.md exists with "Phase 8" on 6 different lines,
      no References/Audit Notes section
    - tests/fixtures/f3_own_id_fp.md exists with YAML frontmatter containing
      phase: "08" and plan: "02", body containing "08-02", no audit section
    - test_f3_dedup_fires_once FAILS against unmodified source
    - test_f3_own_id_suppressed_via_frontmatter FAILS against unmodified source
    - All 8 pre-existing F3 tests still pass
    - Step 0 clean on all modified/new files
  </acceptance_criteria>

  <action>
    All edits inside .worktrees/f3-per-mention-dedup/.

    FIXTURE f3_dedup_fp.md: A plan that references "Phase 8" six times on
    different lines. Must have section headings (so the parser produces
    sections) but NO section heading matching _EXEMPT_KEYWORDS (no
    "References", "Audit Notes", "Appendix", etc.). The six "Phase 8"
    occurrences must be in body text across multiple sections.

    Exact content:

      # Dedup False-Positive Fixture

      ## Overview

      This plan depends on Phase 8 for the parser foundation.

      Phase 8 introduced the section-aware parser that all gates rely on.

      ## Design

      The architecture follows Phase 8 conventions for gate structure.

      We reuse the Phase 8 pattern of regex-based claim extraction.

      ## Implementation

      Import the Phase 8 utilities for section traversal.

      Align error messages with the Phase 8 format for consistency.

    Six occurrences of "Phase 8" across three sections. No exempt section
    heading. Pre-fix: _PHASE_RE matches all six, seen key (claim, lineno)
    produces 6 distinct keys, 6 findings. Post-fix: seen key claim.lower()
    produces 1 key ("phase 8"), first occurrence fires, 5 skipped.

    FIXTURE f3_own_id_fp.md: A GSD-format PLAN.md with YAML frontmatter
    containing phase and plan fields. The body references its own plan ID
    "08-02" which _SUBPLAN_RE matches. Current _own_id_set returns empty
    (no ATX heading with an ID), so 08-02 fires as an unverified cross-plan
    ref. After the fix, frontmatter extraction adds "08-02" to own set.

    Exact content:

      ---
      phase: "08"
      plan: "02"
      type: execute
      wave: 1
      depends_on: []
      files_modified: []
      autonomous: true
      ---

      <objective>
      Implement the 08-02 parser extension.
      </objective>

      ## Overview

      This plan (08-02) adds frontmatter parsing to the gate.

    The YAML frontmatter has phase: "08" and plan: "02". The body contains
    "08-02" twice. _SUBPLAN_RE matches "08-02". Pre-fix: _own_id_set finds
    no ATX heading (first non-empty line is "---"), returns empty set, so
    08-02 fires. Post-fix: frontmatter scan extracts phase "08" + plan "02"
    -> own.add("08-02"), body matches are exempted.

    TEST STUBS: Add two functions after test_f3_cross_ref_still_fires
    (end of test_f3_cross_plan_invariant.py). Use the exact function
    signatures and assertion logic from RESEARCH.md:

      def test_f3_dedup_fires_once():
          """Dedup: Phase 8 referenced 6 times must produce exactly 1 F3 finding."""
          parsed = parse(_load("f3_dedup_fp.md"))
          findings = f3_cross_plan_invariant.check(parsed)
          f3_findings = [f for f in findings if f.check_id == "F3.unverified_cross_plan_ref"]
          phase8_findings = [f for f in f3_findings if "Phase 8" in f.message or "phase 8" in f.message]
          assert len(phase8_findings) == 1, (
              f"Expected exactly 1 F3 finding for Phase 8, got {len(phase8_findings)}: "
              f"{[f.message for f in phase8_findings]}"
          )


      def test_f3_own_id_suppressed_via_frontmatter():
          """Own-ID from YAML frontmatter must suppress self-references."""
          parsed = parse(_load("f3_own_id_fp.md"))
          findings = f3_cross_plan_invariant.check(parsed)
          own_id_findings = [
              f for f in findings
              if f.check_id == "F3.unverified_cross_plan_ref" and "08-02" in f.message
          ]
          assert own_id_findings == [], (
              f"Own id 08-02 from frontmatter should be exempt, got: {own_id_findings}"
          )

    Before adding stubs, verify RED state by temporarily creating the fixture
    and running a quick check to confirm findings fire against unmodified source.
    f3_dedup_fp.md should produce 6 F3 findings for "Phase 8".
    f3_own_id_fp.md should produce at least 1 F3 finding for "08-02".

    Step 0 pre-check on all new/modified files (run from worktree root):
      python3 -m py_compile tests/unit/test_f3_cross_plan_invariant.py
      ruff check tests/unit/test_f3_cross_plan_invariant.py
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -P '[^\x00-\x7F]' && echo "FAIL: non-ASCII" || echo "OK"
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]|P[0-9]+:|S[0-9]+:' &&
        echo "FAIL: AI smell" || echo "OK"

    Confirm RED: run both new tests in isolation -- both must FAIL.
      uv run pytest tests/unit/test_f3_cross_plan_invariant.py::test_f3_dedup_fires_once -xvs
      uv run pytest tests/unit/test_f3_cross_plan_invariant.py::test_f3_own_id_suppressed_via_frontmatter -xvs

    Then run the full F3 test file to confirm pre-existing tests still pass:
      uv run pytest tests/unit/test_f3_cross_plan_invariant.py -q
    Expected: 8 passed, 2 failed.
  </action>

  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/f3-per-mention-dedup && uv run pytest
      tests/unit/test_f3_cross_plan_invariant.py -q 2>&1 | tail -10
    </automated>
  </verify>

  <done>
    Two fixture files created. test_f3_dedup_fires_once and
    test_f3_own_id_suppressed_via_frontmatter both FAIL against unmodified
    source (RED confirmed). All 8 pre-existing F3 tests still pass. Step 0
    clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture markdown -> parser | Fixed-content .md files; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-SC | Tampering | no new packages installed | accept | no package manager installs in this plan; legitimacy gate not applicable |
</threat_model>

<verification>
From inside .worktrees/f3-per-mention-dedup/:

  uv run pytest tests/unit/test_f3_cross_plan_invariant.py -q

Expected: 8 passed, 2 failed -- test_f3_dedup_fires_once and
test_f3_own_id_suppressed_via_frontmatter in FAILED state. All 8
pre-existing F3 tests pass.
</verification>

<success_criteria>
1. git worktree list shows .worktrees/f3-per-mention-dedup at fix/f3-dedup-v013
2. tests/fixtures/f3_dedup_fp.md and f3_own_id_fp.md exist
3. test_f3_dedup_fires_once: FAILED (6 findings, assertion expects 1)
4. test_f3_own_id_suppressed_via_frontmatter: FAILED (08-02 finding present, assertion expects [])
5. All 8 pre-existing F3 tests: PASSED
6. Step 0 clean on modified test file
7. Main worktree git status: clean (no modifications)
</success_criteria>

<output>
Create .planning/phases/11-f3-per-mention-dedup-and-own-id-suppression/11-01-SUMMARY.md when done.
</output>
