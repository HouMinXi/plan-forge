---
phase: "12"
plan: "03"
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - .planning/phases/12-f3-phase10-and-phase-text-suppression/12-REVIEW-RESULT.md
  - src/plan_forge/__init__.py
  - pyproject.toml
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - F3-03
  - F3-04

must_haves:
  truths:
    - "Forge review pipeline completed: 3 cycles, 9 passes minimum, zero findings in final cycle"
    - "Step 4 smoke test passed on fix/f3-phase10-text-v014 after review"
    - "Branch fix/f3-phase10-text-v014 merged into main"
    - "Worktree .worktrees/fix-f3-phase10-text-v014 removed after merge"
    - "Version bumped to 0.1.4 in __init__.py and pyproject.toml"
    - "Full suite passes on main after merge with all 13 F3 tests PASSED"
    - "ROADMAP.md: Phase 12 plans marked complete, progress table updated"
  artifacts:
    - path: "12-REVIEW-RESULT.md"
      provides: "forge review outcome gate (must contain PASS before merge proceeds)"
    - path: "src/plan_forge/__init__.py"
      provides: "version string bumped to 0.1.4"
    - path: "pyproject.toml"
      provides: "project version bumped to 0.1.4"
  key_links:
    - from: "forge review sub-session"
      to: "f3_cross_plan_invariant.py diff"
      via: "human forwards /tmp/draft_forge_review_phase12.txt to separate Claude session"
      pattern: "review sub-session separate from implementation"
    - from: "12-REVIEW-RESULT.md"
      to: "Task 3 merge gate"
      via: "executor reads file and checks for PASS string before merging"
      pattern: "PASS"
---

<objective>
Run the full forge review pipeline on the Phase 12 diff, then merge to main,
bump the version to 0.1.4, clean up the worktree, and update ROADMAP.md.

Purpose: CLAUDE.md requires implementation and review agents to be separate
sub-sessions. This plan is a checkpoint that pauses for the user to forward
the review prompt to a separate Claude session, await zero findings, then
merge. Version bump follows the v0.1.4 milestone goal in ROADMAP.md.

Output: Phase 12 changes merged to main; fix/f3-phase10-text-v014 branch
retired; worktree removed; version 0.1.4 tagged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/houminxi/code/plan-forge/.planning/phases/12-f3-phase10-and-phase-text-suppression/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare review prompt and run Step 0</name>
  <files>/tmp/draft_forge_review_phase12.txt (review prompt file)</files>

  <read_first>
    - Run: git -C /home/houminxi/code/plan-forge diff main...fix/f3-phase10-text-v014
      (full diff of all Phase 12 changes: F3 fix + fixtures + tests)
    - /home/houminxi/code/plan-forge/CLAUDE.md section "Code Quality Gates"
      (review pipeline requirements: Step 0 then 3-cycle static review,
      implementation agent != reviewer agent)
  </read_first>

  <acceptance_criteria>
    - Step 0a (python3 -m py_compile) clean on f3_cross_plan_invariant.py
    - Step 0b (ruff check) clean on f3_cross_plan_invariant.py
    - Step 0c (non-ASCII grep on diff) returns nothing
    - Step 0d (AI-smell grep on diff) returns nothing
    - /tmp/draft_forge_review_phase12.txt exists with full diff and review instructions
  </acceptance_criteria>

  <action>
    From inside .worktrees/fix-f3-phase10-text-v014/, run Step 0 on the
    combined diff against main:

    0a Syntax:
      python3 -m py_compile src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py

    0b Lint:
      ruff check src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py

    0c Non-ASCII:
      git diff main...fix/f3-phase10-text-v014 --diff-filter=AM -U0 |
        grep '^+' | grep -P '[^\x00-\x7F]' &&
        echo "FAIL: non-ASCII" || echo "OK"

    0d AI-smell:
      git diff main...fix/f3-phase10-text-v014 --diff-filter=AM -U0 |
        grep '^+' |
        grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]|P[0-9]+:|S[0-9]+:' &&
        echo "FAIL: AI smell" || echo "OK"

    If any check fails, fix in the worktree before proceeding to forge review.

    Prepare the forge review prompt and write to /tmp/draft_forge_review_phase12.txt:

      Review the following diff for plan-forge Phase 12 changes.
      Apply the full forge review pipeline (qodo-review, code-review-expert,
      adversarial-qe -- 3 cycles, 9 passes minimum). Report all findings.
      Exit criteria: all 3 passes in the final cycle produce zero findings.

      [paste full git diff output here]

    Output the path /tmp/draft_forge_review_phase12.txt for the user to
    forward to a separate review sub-session.
  </action>

  <verify>
    <automated>
      git -C /home/houminxi/code/plan-forge diff main...fix/f3-phase10-text-v014
      --stat 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    Step 0 all clean. Review prompt written to /tmp/draft_forge_review_phase12.txt.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Forward to review sub-session and await zero findings</name>

  <what-built>
    Review prompt is at /tmp/draft_forge_review_phase12.txt with the full
    Phase 12 diff. Step 0 is clean. This session is the implementation session;
    the review must happen in a SEPARATE Claude session.
  </what-built>

  <how-to-verify>
    1. Open a new Claude session (separate from this implementation session).
    2. Paste the contents of /tmp/draft_forge_review_phase12.txt into the new
       session.
    3. Have the review session run qodo-review, code-review-expert, and
       adversarial-qe -- 3 full cycles, 9 passes minimum.
    4. For each finding: fix in the implementation worktree
       (.worktrees/fix-f3-phase10-text-v014/), re-run Step 0, re-run tests,
       then bring the updated diff to the review session for re-review.
       Do not shortcut cycles.
    5. Exit criteria: all 3 passes in the final cycle report zero findings.
    6. After zero findings: run Step 4 smoke test in the implementation session:
         uv run pytest tests/ -q
       Must pass.
    7. Have the review sub-session write its final result to:
         .planning/phases/12-f3-phase10-and-phase-text-suppression/12-REVIEW-RESULT.md
       The file must contain "PASS: 3 cycles zero findings" or list remaining
       findings if not yet clean. Task 3 reads this file before merging.
  </how-to-verify>

  <resume-signal>
    Type "forge-review-clean: 3 cycles zero findings, Step 4 smoke test passed"
    to resume and proceed to merge.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Version bump, merge branch, remove worktree, update ROADMAP</name>
  <files>
    src/plan_forge/__init__.py,
    pyproject.toml,
    .planning/phases/12-f3-phase10-and-phase-text-suppression/12-REVIEW-RESULT.md (read-only gate),
    .planning/ROADMAP.md
  </files>

  <read_first>
    - /home/houminxi/code/plan-forge/.planning/phases/12-f3-phase10-and-phase-text-suppression/12-REVIEW-RESULT.md
      (must exist and contain "PASS" or "zero findings" before proceeding;
      if absent or shows remaining findings, do not merge -- return to Task 2)
    - /home/houminxi/code/plan-forge/src/plan_forge/__init__.py
      (locate the __version__ string; confirm current value is "0.1.3")
    - /home/houminxi/code/plan-forge/pyproject.toml
      (locate the version field under [project]; confirm current value is "0.1.3")
    - Run: git -C /home/houminxi/code/plan-forge log fix/f3-phase10-text-v014 --oneline
      (confirm all Phase 12 commits are on branch before merge)
  </read_first>

  <acceptance_criteria>
    - 12-REVIEW-RESULT.md contains "PASS" or "zero findings" (gate check)
    - src/plan_forge/__init__.py contains __version__ = "0.1.4"
    - pyproject.toml contains version = "0.1.4" under [project]
    - Version bump is committed on fix/f3-phase10-text-v014 before merge
    - Merge commit on main contains all Phase 12 changes
    - Full suite passes on main after merge (all 13 F3 tests pass)
    - Worktree .worktrees/fix-f3-phase10-text-v014 removed
    - Branch fix/f3-phase10-text-v014 deleted
    - ROADMAP.md: all 3 Phase 12 plan checkboxes marked [x], progress table
      shows Phase 12 Complete with date, v0.1.4 milestone noted
  </acceptance_criteria>

  <action>
    PART A -- Gate check. Read 12-REVIEW-RESULT.md. If it does not contain
    "PASS" or "zero findings", stop and return to Task 2. Do not proceed.

    PART B -- Version bump. From inside .worktrees/fix-f3-phase10-text-v014/:

    Edit src/plan_forge/__init__.py: change __version__ = "0.1.3" to
    __version__ = "0.1.4". If the version string uses a different format,
    match it but change the numeric value to 0.1.4.

    Edit pyproject.toml: change version = "0.1.3" to version = "0.1.4"
    under the [project] section.

    Stage and create a version-bump commit on the feature branch with a
    human-voice message. No AI smell, no task IDs. Example:

      chore: bump version to 0.1.4

      Signed-off-by: Minxi Hou <houminxi@gmail.com>

    Mark the commit with # chore (non-code change).

    PART C -- Merge. From /home/houminxi/code/plan-forge (main repo root),
    NOT from inside the worktree:

      git checkout main
      git merge --no-ff fix/f3-phase10-text-v014

    Use a merge commit message summarizing the fix without AI smell:
      checks/f3: Phase 10+ ID matching and own-phase-text suppression (v0.1.4)

    Verify the merged state:
      uv run pytest tests/ -q

    Full suite must pass on main after merge. All 13 F3 tests must be PASSED.

    PART D -- Clean up:
      git worktree remove .worktrees/fix-f3-phase10-text-v014
      git branch -d fix/f3-phase10-text-v014

    PART E -- Update ROADMAP.md on main. Mark the three Phase 12 plan
    checkboxes as complete:
      - [x] 12-01-PLAN.md -- Worktree setup + fixture scaffolding + failing test stubs (RED)
      - [x] 12-02-PLAN.md -- _SUBPLAN_RE extension + phase-text token suppression + bite test
      - [x] 12-03-PLAN.md -- Forge review pipeline + merge + worktree cleanup

    Update the progress table row for Phase 12:
      | 12. F3 Phase 10+ and Phase-Text | v0.1.4 | 3/3 | Complete | 2026-05-31 |

    Update the Milestones section to note v0.1.4 shipped.

    Stage and create a docs commit on main with # docs marker. Human-voice
    message, no AI smell:

      docs: mark Phase 12 complete, ship v0.1.4

      Signed-off-by: Minxi Hou <houminxi@gmail.com>
  </action>

  <verify>
    <automated>
      grep -iE 'PASS|zero findings'         /home/houminxi/code/plan-forge/.planning/phases/12-f3-phase10-and-phase-text-suppression/12-REVIEW-RESULT.md &&
      git -C /home/houminxi/code/plan-forge worktree list &&
      git -C /home/houminxi/code/plan-forge log --oneline -5
    </automated>
  </verify>

  <done>
    12-REVIEW-RESULT.md confirmed "PASS" before merge. fix/f3-phase10-text-v014
    merged to main. Version bumped to 0.1.4 in __init__.py and pyproject.toml.
    Worktree removed. Branch deleted. Full suite passes on main with all 13 F3
    tests PASSED. ROADMAP.md updated: Phase 12 complete, v0.1.4 shipped.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review sub-session -> findings | Separate Claude session; findings are advisory until human confirms |
| 12-REVIEW-RESULT.md -> merge gate | File written by review sub-session; implementation session reads it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Repudiation | forge review bypassed | mitigate | checkpoint:human-action gate; resume-signal documents review outcome; impl != reviewer enforced by separate session requirement |
| T-12-SC | Tampering | no new packages installed | accept | no package manager installs in this plan |
</threat_model>

<verification>
After merge:

  git -C /home/houminxi/code/plan-forge log --oneline -5
  (should show: ROADMAP update, merge commit, version bump, F3 fix commit)

  uv run pytest /home/houminxi/code/plan-forge/tests/ -q
  (all tests pass; all 13 F3 tests included)

  git -C /home/houminxi/code/plan-forge worktree list
  (only main worktree listed; fix-f3-phase10-text-v014 removed)

  grep '__version__' /home/houminxi/code/plan-forge/src/plan_forge/__init__.py
  (output: __version__ = "0.1.4")
</verification>

<success_criteria>
1. Forge review: 3 cycles, 9 passes, zero findings in final cycle
2. Step 4 smoke test: uv run pytest tests/ -q passes on fix/f3-phase10-text-v014
   after review cycle
3. __version__ = "0.1.4" in src/plan_forge/__init__.py
4. version = "0.1.4" in pyproject.toml under [project]
5. Merge commit on main contains all Phase 12 F3 changes
6. Full suite passes on main (all 13 F3 tests PASSED)
7. Worktree .worktrees/fix-f3-phase10-text-v014 removed
8. Branch fix/f3-phase10-text-v014 deleted
9. ROADMAP.md: Phase 12 all 3 plans [x], progress table Complete, v0.1.4 shipped
</success_criteria>

<output>
Create .planning/phases/12-f3-phase10-and-phase-text-suppression/12-03-SUMMARY.md when done.
</output>
