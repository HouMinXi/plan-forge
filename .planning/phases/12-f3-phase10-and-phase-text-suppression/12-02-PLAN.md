---
phase: "12"
plan: "02"
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py
autonomous: false
requirements:
  - F3-03
  - F3-04

must_haves:
  truths:
    - "test_f3_phase10_cross_ref_detected PASSES (GREEN: 1 finding for 10-02)"
    - "test_f3_phase10_own_id_suppressed PASSES (GREEN: own-ID suppression works end-to-end for Phase 10)"
    - "test_f3_phase_text_self_ref_suppressed PASSES (GREEN: Phase 8 and Phase-8 suppressed, Phase 9 fires)"
    - "All 10 pre-existing F3 tests still pass (no regression)"
    - "Stale comment at lines 39-42 is removed from f3_cross_plan_invariant.py"
    - "Bug-inject cycle completed: revert _SUBPLAN_RE to old regex, test_f3_phase10_cross_ref_detected fails; restore fix, GREEN"
    - "Full test suite green"
  artifacts:
    - path: "src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py"
      provides: "F3 check with extended _SUBPLAN_RE and phase-text token suppression in _own_id_set()"
      contains: "(?:0[1-9]|[1-9]\\d)-\\d{2}"
  key_links:
    - from: "f3_cross_plan_invariant.py _SUBPLAN_RE"
      to: "_extract_all_claims() and heading scan in _own_id_set()"
      via: "single constant used in both functions automatically"
      pattern: "_SUBPLAN_RE"
    - from: "f3_cross_plan_invariant.py _own_id_set() phase-text block"
      to: "check() verified set"
      via: "verified.update(_own_id_set(parsed)) at line 134"
      pattern: "phase-text tokens"
---

<objective>
Apply both source changes to f3_cross_plan_invariant.py: extend _SUBPLAN_RE
to cover Phase 10-99 IDs, add phase-text token suppression in _own_id_set(),
and remove the now-stale comment. Run the bite test to confirm both guards
are load-bearing before committing.

Purpose: Plan 12-01 established the RED state. This plan makes both tests
GREEN. The regex change fixes a false negative (Phase 10+ cross-refs were
silently missed). The phase-text block fixes a false positive (a plan
mentioning its own "Phase N" label was incorrectly flagged). The stale
comment (lines 39-42) claimed the regex extension was deferred -- that claim
is now wrong and must be removed.

Output: Modified f3_cross_plan_invariant.py with all 13 F3 tests GREEN;
bite test confirmed; pre-review commit on fix/f3-phase10-text-v014.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/houminxi/code/plan-forge/.planning/phases/12-f3-phase10-and-phase-text-suppression/12-CONTEXT.md
@/home/houminxi/code/plan-forge/.planning/phases/12-f3-phase10-and-phase-text-suppression/12-RESEARCH.md
@/home/houminxi/code/plan-forge/.planning/phases/12-f3-phase10-and-phase-text-suppression/12-01-SUMMARY.md

<interfaces>
From src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py
(current source, read the full file before editing):

  CHANGE 1 -- Line 24 (_SUBPLAN_RE constant):

    BEFORE:
      _SUBPLAN_RE = re.compile(r'(?<!\d-)\b0[1-9]-\d{2}\b')

    AFTER:
      _SUBPLAN_RE = re.compile(r'(?<!\d-)\b(?:0[1-9]|[1-9]\d)-\d{2}\b')

    The lookbehind (?<!\d-) is unchanged (fixed length 2, required by Python re).
    The phase prefix changes from [0[1-9]] to [(?:0[1-9]|[1-9]\d)] covering 01-99.
    This constant is used in both _extract_all_claims() and the heading scan in
    _own_id_set() -- a single constant change covers both sites automatically.

  CHANGE 2 -- Lines 39-42 (stale comment, DELETE THESE 4 LINES):

    # Note: Phase 10+ IDs (two-digit phase numbers like 10-02) are not matched
    # by _SUBPLAN_RE in document body, so frontmatter extraction adds them to
    # the verified set but suppression has no practical effect on body scanning.
    # Extending the subplan pattern to cover Phase 10+ is deferred.

    After this deletion, line 43 (currently _BOM = chr(0xFEFF)) becomes the
    new line immediately following the _EXEMPT_KEYWORDS block.

  CHANGE 3 -- _own_id_set() (insert phase-text block BEFORE plan_num guard):

    BEFORE (lines 90-91):
      if phase_num and plan_num:
          own.add(f"{phase_num}-{plan_num}")

    AFTER (insert the phase-text block, then keep the existing guard):
      if phase_num:
          phase_int = int(phase_num)
          own.add(f"phase {phase_int}")
          own.add(f"phase-{phase_int}")
          own.add(f"phase {phase_num}")
          own.add(f"phase-{phase_num}")

      if phase_num and plan_num:
          own.add(f"{phase_num}-{plan_num}")

    The phase-text block MUST precede the plan_num guard so a plan with
    phase: 8 but no plan: field still gets phase-text suppression.

    For phase_num="08": int("08")=8 so tokens are "phase 8", "phase-8",
    "phase 08", "phase-08" (4 distinct tokens, matching all _PHASE_RE forms).
    For phase_num="10": int("10")=10 so tokens are "phase 10", "phase-10",
    "phase 10", "phase-10" -- set absorbs the 2 duplicates (padded==unpadded
    for two-digit numbers), harmless.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply _SUBPLAN_RE extension, phase-text block, and remove stale comment</name>
  <files>src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py</files>

  <read_first>
    - /home/houminxi/code/plan-forge/src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py
      (full file; confirm: line 24 is _SUBPLAN_RE, lines 39-42 are the stale
      comment, lines 80-91 are the _own_id_set frontmatter loop ending with
      the plan_num guard; read all 164 lines before editing)
    - /home/houminxi/code/plan-forge/.planning/phases/12-f3-phase10-and-phase-text-suppression/12-01-SUMMARY.md
      (confirm Plan 01 baseline: exact RED test counts confirmed)
    - /home/houminxi/code/plan-forge/tests/fixtures/f3_dedup_fp.md
      (no frontmatter; D-04 phase-text block never runs for it; must remain
      unaffected -- 1 finding for "Phase 8" expected after fix, unchanged)
  </read_first>

  <behavior>
    After all three changes:

    test_f3_phase10_cross_ref_detected: PASSES. Extended _SUBPLAN_RE matches
    "10-02" in the body of f3_phase10_cross_ref.md. No frontmatter, no audit
    notes. check() returns 1 F3 HIGH with "10-02" in message.

    test_f3_phase10_own_id_suppressed: PASSES. Extended _SUBPLAN_RE now matches
    "10-02" in the body of f3_phase10_own_id_fp.md AND in the heading "# 10-02
    Schema Migration". _own_id_set() adds "10-02" to own via frontmatter
    (phase_num="10" zfill(2)="10", plan_num="2" zfill(2)="02"). check() returns
    0 findings (own-ID suppression).

    test_f3_phase_text_self_ref_suppressed: PASSES. D-04 block adds "phase 8",
    "phase-8", "phase 08", "phase-08" to own for f3_phase_text_fp.md (phase:8
    frontmatter). "Phase 8" and "Phase-8" in body are both in verified (lowercased).
    "Phase 9" is not in own and fires. check() returns exactly 1 F3 finding
    for "Phase 9".

    test_f3_dedup_fires_once: PASSES unchanged. f3_dedup_fp.md has no frontmatter,
    so D-04 block never runs; "Phase 8" still fires exactly 1 finding (dedup).

    All 10 pre-existing F3 tests: PASS (no regression).
  </behavior>

  <acceptance_criteria>
    - Line 24 of f3_cross_plan_invariant.py reads exactly:
      _SUBPLAN_RE = re.compile(r'(?<!\d-)\b(?:0[1-9]|[1-9]\d)-\d{2}\b')
    - Lines 39-42 (the 4-line stale comment block starting with
      "# Note: Phase 10+ IDs") are absent from the file
    - _own_id_set() contains an "if phase_num:" block that calls int(phase_num)
      and adds 4 phase-text tokens to own, placed BEFORE the "if phase_num and plan_num:" guard
    - All 13 F3 tests pass (10 pre-existing + 3 new from Plan 12-01)
    - python3 -m py_compile src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py: clean
    - ruff check src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py: clean
    - Non-ASCII check on diff: no matches
    - AI-smell grep on diff: no matches
  </acceptance_criteria>

  <action>
    Edit inside .worktrees/fix-f3-phase10-text-v014/ only.

    CHANGE 1: Replace line 24 _SUBPLAN_RE.
    Change only this one line. The comment on lines 21-23 ("The _SUBPLAN_RE
    includes...") is still accurate (lookbehind is still present) and must
    be kept. Do not alter lines 21-23.

      FROM: _SUBPLAN_RE = re.compile(r'(?<!\d-)\b0[1-9]-\d{2}\b')
      TO:   _SUBPLAN_RE = re.compile(r'(?<!\d-)\b(?:0[1-9]|[1-9]\d)-\d{2}\b')

    CHANGE 2: Delete the 4-line stale comment at lines 39-42.
    The block to delete is:
      # Note: Phase 10+ IDs (two-digit phase numbers like 10-02) are not matched
      # by _SUBPLAN_RE in document body, so frontmatter extraction adds them to
      # the verified set but suppression has no practical effect on body scanning.
      # Extending the subplan pattern to cover Phase 10+ is deferred.

    After deletion, _BOM = chr(0xFEFF) immediately follows the _EXEMPT_KEYWORDS
    block with one blank line between them. Preserve the blank line structure
    of the file; do not collapse or add blank lines beyond what the deletion removes.

    CHANGE 3: Insert phase-text block inside _own_id_set() before the plan_num guard.
    Locate the existing block at lines 90-91:
      if phase_num and plan_num:
          own.add(f"{phase_num}-{plan_num}")

    Insert the following block immediately before it (preserving 4-space indentation
    matching the surrounding code):

          if phase_num:
              phase_int = int(phase_num)
              own.add(f"phase {phase_int}")
              own.add(f"phase-{phase_int}")
              own.add(f"phase {phase_num}")
              own.add(f"phase-{phase_num}")

    Leave one blank line between the new block and the existing
    "if phase_num and plan_num:" guard.

    Step 0 pre-check (run from inside worktree root):

      python3 -m py_compile src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py

      ruff check src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py

      git diff --diff-filter=AM -U0 | grep '^+' | \
        grep -P '[^\x00-\x7F]' && echo "FAIL: non-ASCII" || echo "OK"

      git diff --diff-filter=AM -U0 | grep '^+' | \
        grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]|P[0-9]+:|S[0-9]+:' && \
        echo "FAIL: AI smell" || echo "OK"

    Run F3 tests to confirm GREEN:

      uv run pytest tests/unit/test_f3_cross_plan_invariant.py -q

    Expected: 13 passed, 0 failed.
  </action>

  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/fix-f3-phase10-text-v014 && \
      uv run pytest tests/unit/test_f3_cross_plan_invariant.py -q 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    All 13 F3 tests pass including the 3 new tests (GREEN). Stale comment
    is absent from the source file. Step 0 clean on modified source.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bite test -- verify both guards are load-bearing</name>

  <what-built>
    F3 source fix is GREEN (13 tests pass). Before committing, perform a
    bite test to confirm each new guard is the operative suppression -- not
    that the test infrastructure is accidentally lenient.
  </what-built>

  <how-to-verify>
    Perform from inside .worktrees/fix-f3-phase10-text-v014/.

    STEP A -- Temporarily revert _SUBPLAN_RE to the old pattern:
    In f3_cross_plan_invariant.py, change line 24 back to:
      _SUBPLAN_RE = re.compile(r'(?<!\d-)\b0[1-9]-\d{2}\b')

    STEP B -- Run the Phase 10 cross-ref test:
      uv run pytest tests/unit/test_f3_cross_plan_invariant.py::test_f3_phase10_cross_ref_detected -xvs
    Expected: FAILED (0 findings returned; "10-02" not matched by old regex)

    Also confirm own-ID test no longer regressess (it should still pass because
    the old regex also misses "10-02", so 0 findings, and test asserts []):
      uv run pytest tests/unit/test_f3_cross_plan_invariant.py::test_f3_phase10_own_id_suppressed -xvs
    Expected: PASSED (trivially -- old regex also returns 0 findings)

    STEP C -- Restore _SUBPLAN_RE fix:
      git checkout -- src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py

    STEP D -- Confirm fix is back:
      uv run pytest tests/unit/test_f3_cross_plan_invariant.py -q
    Expected: 13 passed, 0 failed.

    STEP E -- Temporarily comment out the phase-text block in _own_id_set():
    Comment out the 5-line "if phase_num:" block (the new D-04 block only;
    leave the existing "if phase_num and plan_num:" guard untouched).

    STEP F -- Run the phase-text test:
      uv run pytest tests/unit/test_f3_cross_plan_invariant.py::test_f3_phase_text_self_ref_suppressed -xvs
    Expected: FAILED (2+ findings returned; "Phase 8" and "Phase-8" fire
    because own-phase tokens were not added to verified)

    STEP G -- Restore:
      git checkout -- src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py

    STEP H -- Final GREEN confirmation:
      uv run pytest tests/unit/test_f3_cross_plan_invariant.py -q
    Expected: 13 passed, 0 failed.

    Record the outcome for the commit message body.
  </how-to-verify>

  <resume-signal>
    Type "bite-test-confirmed: regex revert causes cross-ref test to fail;
    phase-text block removal causes phase-text test to fail; both guards
    load-bearing; 13 tests green after restore" then proceed to Task 3.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Full suite green and commit F3 fix</name>
  <files>src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py</files>

  <read_first>
    - Result of Task 2 bite test (finding counts before/after each guard removal)
    - git -C /home/houminxi/code/plan-forge log --oneline -3 (commit style reference)
  </read_first>

  <acceptance_criteria>
    - Full test suite passes (all tests, minus any pre-existing unrelated failures)
    - Commit is on fix/f3-phase10-text-v014 with human-voice message
    - Commit message contains no task IDs, no model names, no review metadata,
      no Co-Authored-By AI lines
    - Commit message body records what the bite test showed in human-voice prose
    - Signed-off-by: Minxi Hou <houminxi@gmail.com>
  </acceptance_criteria>

  <action>
    From inside .worktrees/fix-f3-phase10-text-v014/, run full suite:
      uv run pytest tests/ -q

    All tests must pass (pre-existing count plus 3 new F3 tests).
    If any pre-existing test that was passing before this plan now fails,
    stop and diagnose before committing.

    Stage all Phase 12 changes (source fix plus the fixtures and test file
    created in Plan 12-01 which exist in the same worktree):

      git add src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py \
              tests/fixtures/f3_phase10_cross_ref.md \
              tests/fixtures/f3_phase10_own_id_fp.md \
              tests/fixtures/f3_phase_text_fp.md \
              tests/unit/test_f3_cross_plan_invariant.py

    Commit with a human-voice message. No task IDs, no model names, no AI
    attribution. Write WHY, not WHAT. Example structure:

      checks/f3: extend phase-ID regex to cover Phase 10+ and suppress own-phase-text refs

      Phase 10+ subplan IDs (10-02, 11-01, etc.) were silently missed by
      the subplan regex, which only covered the 01-09 prefix range. Plans
      referencing cross-phase work introduced in Phase 10 onward received
      no F3 finding, defeating the audit-coverage check.

      The phase-text false positive was the mirror problem: a plan that
      mentions its own phase number in prose ("This plan is part of Phase 8")
      received an F3 HIGH finding even though the reference is self-describing,
      not an unaudited dependency.

      Extend _SUBPLAN_RE to (?:0[1-9]|[1-9]\d)-\d{2}, covering Phase 01-99.
      Add four phase-text tokens to the own-ID set when frontmatter phase: is
      present, covering "phase N", "phase-N", "phase NN", and "phase-NN" forms
      so all _PHASE_RE match variants are suppressed. Remove the now-wrong
      comment that described the Phase 10+ extension as deferred.

      Bite test confirmed: reverting the regex causes the Phase 10 cross-ref
      fixture to miss a finding; removing the phase-text block causes the
      own-phase-text fixture to fire two false positives. Both guards are
      load-bearing.

      Signed-off-by: Minxi Hou <houminxi@gmail.com>

    This commit is on the feature branch. Plan 12-03 runs the forge review;
    amendment commits may be added before merge if review finds issues.
  </action>

  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/fix-f3-phase10-text-v014 && \
      uv run pytest tests/ -q 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    Full suite green. F3 fix committed on fix/f3-phase10-text-v014. Commit
    message is human-voice with bite-test outcome recorded. No AI smell.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed.raw_text -> _own_id_set phase-text tokens | Internal Python; raw_text from parser, no user input |
| _SUBPLAN_RE extended pattern -> claim matching | Regex change; ISO date lookbehind unchanged, same protection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | _SUBPLAN_RE extension: new matches could over-suppress | mitigate | bite test confirms test_f3_phase10_cross_ref_detected fires when guard absent; f3_fail.md and f3_cross_ref_fires.md TP fixtures still fire after fix (regression guard) |
| T-12-02 | Tampering | phase-text token block: could over-suppress Phase N refs | mitigate | test_f3_phase_text_self_ref_suppressed asserts "Phase 9" still fires in same fixture that suppresses "Phase 8"; scope is own-phase only |
| T-12-SC | Tampering | no new packages installed | accept | no package manager installs; legitimacy gate not applicable |
</threat_model>

<verification>
From inside .worktrees/fix-f3-phase10-text-v014/:

  grep -n '_SUBPLAN_RE' src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py
  (line 24 must contain: (?:0[1-9]|[1-9]\d)-\d{2})

  grep -n 'Phase 10.*deferred\|Extending the subplan' \
    src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py
  (must return nothing -- stale comment removed)

  grep -n 'phase_int' src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py
  (must show the D-04 block present in _own_id_set())

  uv run pytest tests/unit/test_f3_cross_plan_invariant.py -v
  (all 13 must PASS)
</verification>

<success_criteria>
1. _SUBPLAN_RE on line 24 is r'(?<!\d-)\b(?:0[1-9]|[1-9]\d)-\d{2}\b'
2. The 4-line stale comment (lines 39-42 in pre-fix source) is absent
3. _own_id_set() contains "if phase_num:" block with phase_int and 4 token adds,
   placed before the "if phase_num and plan_num:" guard
4. All 13 F3 tests pass (10 pre-existing + 3 new)
5. Full suite: all tests pass, 0 new failures
6. Bite test confirmed: _SUBPLAN_RE revert causes cross-ref test to fail;
   phase-text block removal causes phase-text test to fail
7. Commit on fix/f3-phase10-text-v014 with human-voice message and Signed-off-by
8. Step 0 clean on f3_cross_plan_invariant.py diff
</success_criteria>

<output>
Create .planning/phases/12-f3-phase10-and-phase-text-suppression/12-02-SUMMARY.md when done.
</output>
