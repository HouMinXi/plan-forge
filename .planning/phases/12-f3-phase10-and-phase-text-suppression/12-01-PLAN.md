---
phase: "12"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .worktrees/fix-f3-phase10-text-v014 (worktree created)
  - tests/fixtures/f3_phase10_cross_ref.md
  - tests/fixtures/f3_phase10_own_id_fp.md
  - tests/fixtures/f3_phase_text_fp.md
  - tests/unit/test_f3_cross_plan_invariant.py
autonomous: false
requirements:
  - F3-03
  - F3-04

must_haves:
  truths:
    - "Worktree .worktrees/fix-f3-phase10-text-v014/ is active on branch fix/f3-phase10-text-v014"
    - "Main worktree is untouched (git status clean)"
    - "test_f3_phase10_cross_ref_detected FAILS against unmodified source (RED: 0 findings, expected 1)"
    - "test_f3_phase_text_self_ref_suppressed FAILS against unmodified source (RED: 2+ findings, expected 1)"
    - "test_f3_phase10_own_id_suppressed is GREEN before the fix (regression guard)"
    - "All 10 pre-existing F3 tests still pass"
    - "Three new fixture .md files exist and are parser-valid"
  artifacts:
    - path: ".worktrees/fix-f3-phase10-text-v014"
      provides: "linked worktree for all Phase 12 code changes"
    - path: "tests/fixtures/f3_phase10_cross_ref.md"
      provides: "bare 10-02 reference without frontmatter or audit notes (Change A RED fixture)"
    - path: "tests/fixtures/f3_phase10_own_id_fp.md"
      provides: "phase:10 plan:2 frontmatter, body references 10-02 (own-ID regression guard)"
    - path: "tests/fixtures/f3_phase_text_fp.md"
      provides: "phase:8 frontmatter, body has Phase 8/Phase-8 (suppressed) and Phase 9 (fires)"
    - path: "tests/unit/test_f3_cross_plan_invariant.py"
      provides: "three new test functions added after test_f3_own_id_suppressed_via_frontmatter"
  key_links:
    - from: "tests/unit/test_f3_cross_plan_invariant.py"
      to: "tests/fixtures/f3_phase10_cross_ref.md"
      via: "_load() in test_f3_phase10_cross_ref_detected"
      pattern: "f3_phase10_cross_ref"
    - from: "tests/unit/test_f3_cross_plan_invariant.py"
      to: "tests/fixtures/f3_phase10_own_id_fp.md"
      via: "_load() in test_f3_phase10_own_id_suppressed"
      pattern: "f3_phase10_own_id_fp"
    - from: "tests/unit/test_f3_cross_plan_invariant.py"
      to: "tests/fixtures/f3_phase_text_fp.md"
      via: "_load() in test_f3_phase_text_self_ref_suppressed"
      pattern: "f3_phase_text_fp"
---

<objective>
Create the worktree and write all Wave 1 test scaffolding: three new fixture
files and three new test functions, establishing the RED state that makes the
fix measurable.

Purpose: Before any source code is touched, pin down the exact failing assertions
so the implementation in Plan 12-02 has a clear target. The 10 existing F3 tests
serve as the regression floor that must remain passing throughout.

Output: Active worktree at .worktrees/fix-f3-phase10-text-v014/, three fixture
.md files, and three new test stubs -- two in RED state, one in regression-guard
GREEN state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/houminxi/code/plan-forge/.planning/ROADMAP.md
@/home/houminxi/code/plan-forge/.planning/phases/12-f3-phase10-and-phase-text-suppression/12-CONTEXT.md
@/home/houminxi/code/plan-forge/.planning/phases/12-f3-phase10-and-phase-text-suppression/12-RESEARCH.md

<interfaces>
From src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py (current source):

  Line 24:
    _SUBPLAN_RE = re.compile(r'(?<!\d-)\b0[1-9]-\d{2}\b')

  This regex only matches Phase 01-09 IDs. "10-02" is NOT matched.
  Before the fix: checking a plan body containing bare "10-02" returns
  0 findings (false negative). test_f3_phase10_cross_ref_detected asserts
  len==1 and therefore FAILS in RED state.

  Lines 80-91 (_own_id_set frontmatter loop, post-Phase 11):
    if phase_num and plan_num:
        own.add(f"{phase_num}-{plan_num}")

  Phase-text tokens ("phase 8", "phase-8") are NOT added to own.
  Before the fix: a plan with phase:8 frontmatter and body "Phase 8" and
  "Phase-8" returns 2+ findings (false positives). test_f3_phase_text_self_ref_suppressed
  asserts len==1 with only "Phase 9" in messages; pre-fix result has 2+ findings,
  so len==1 FAILS in RED state.

  Lines 39-42 (stale comment -- Plan 12-02 must delete these 4 lines):
    # Note: Phase 10+ IDs (two-digit phase numbers like 10-02) are not matched
    # by _SUBPLAN_RE in document body, so frontmatter extraction adds them to
    # the verified set but suppression has no practical effect on body scanning.
    # Extending the subplan pattern to cover Phase 10+ is deferred.

From tests/unit/test_f3_cross_plan_invariant.py (existing pattern):

  _FIXTURES = pathlib.Path(__file__).parent.parent / "fixtures"

  def _load(name: str) -> str:
      return (_FIXTURES / name).read_text(encoding="utf-8")

New functions must follow this exact pattern and be appended after
the last existing function test_f3_own_id_suppressed_via_frontmatter.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create worktree and verify baseline</name>
  <files>.worktrees/fix-f3-phase10-text-v014 (worktree only; no main-tree files changed)</files>

  <read_first>
    - /home/houminxi/code/plan-forge/.gitignore (verify .worktrees/ entry exists)
    - Run: git -C /home/houminxi/code/plan-forge worktree list
    - Run inside /home/houminxi/code/plan-forge: uv run pytest
        tests/unit/test_f3_cross_plan_invariant.py -q
      (baseline: 10 passed per RESEARCH.md Section 2 "All 10 existing test functions")
  </read_first>

  <acceptance_criteria>
    - git worktree list shows .worktrees/fix-f3-phase10-text-v014 at branch fix/f3-phase10-text-v014
    - Baseline F3 test count is exactly 10 passed, 0 failed
    - Main worktree git status is clean (no staged or unstaged changes)
  </acceptance_criteria>

  <action>
    From /home/houminxi/code/plan-forge (main repo root), run:

      git worktree add .worktrees/fix-f3-phase10-text-v014 -b fix/f3-phase10-text-v014

    This creates a linked worktree on a new branch. All file edits in Plans
    12-01, 12-02, and 12-03 happen inside .worktrees/fix-f3-phase10-text-v014/,
    never in the main worktree.

    Verify .worktrees/ is in .gitignore. If the entry is absent, append
    ".worktrees/" to .gitignore from inside the worktree.

    From inside .worktrees/fix-f3-phase10-text-v014/, run:
      uv run pytest tests/unit/test_f3_cross_plan_invariant.py -q

    Record the exact count. Expected: 10 passed, 0 failed. If count differs,
    stop and report before proceeding.
  </action>

  <verify>
    <automated>git -C /home/houminxi/code/plan-forge worktree list | grep fix-f3-phase10-text-v014</automated>
  </verify>

  <done>
    Worktree .worktrees/fix-f3-phase10-text-v014/ exists at branch
    fix/f3-phase10-text-v014. Baseline: 10 F3 tests passed, 0 failed.
    Main worktree is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixture files and failing test stubs (RED)</name>
  <files>
    tests/fixtures/f3_phase10_cross_ref.md,
    tests/fixtures/f3_phase10_own_id_fp.md,
    tests/fixtures/f3_phase_text_fp.md,
    tests/unit/test_f3_cross_plan_invariant.py
  </files>

  <read_first>
    - /home/houminxi/code/plan-forge/src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py
      (full file; understand _SUBPLAN_RE line 24, _own_id_set() lines 55-101,
      check() verified-set assembly lines 126-134; note the 4-line stale comment
      at lines 39-42 that Plan 12-02 must remove)
    - /home/houminxi/code/plan-forge/tests/fixtures/f3_own_id_fp.md
      (Phase 11 own-ID fixture: phase:"08" plan:"02" -- structural template
      for f3_phase10_own_id_fp.md)
    - /home/houminxi/code/plan-forge/tests/fixtures/f3_dedup_fp.md
      (shows "Phase 8" six times without frontmatter produces 1 finding;
      confirms _PHASE_RE fires on phase-text claims in body)
    - /home/houminxi/code/plan-forge/tests/unit/test_f3_cross_plan_invariant.py
      (full file; locate test_f3_own_id_suppressed_via_frontmatter as the
      insertion point for the three new functions)
  </read_first>

  <behavior>
    Against unmodified source (before Plan 12-02 fix):

    test_f3_phase10_cross_ref_detected: asserts len(findings)==1 and "10-02"
    in findings[0].message. Current _SUBPLAN_RE does not match "10-02" (only
    01-09 prefix IDs), so check() returns 0 findings. Assertion len==1 FAILS.
    RED gate for F3-03.

    test_f3_phase10_own_id_suppressed: asserts findings==[]. Before fix,
    _SUBPLAN_RE also misses "10-02" in f3_phase10_own_id_fp.md body, so
    0 findings returned. Assertion findings==[] PASSES. Regression guard:
    after Plan 12-02 extends regex, own-ID suppression must prevent firing.

    test_f3_phase_text_self_ref_suppressed: asserts len(f3_findings)==1 and
    "Phase 9" in finding and "Phase 8" absent. Before fix, _own_id_set()
    does not add phase-text tokens, so "Phase 8" and "Phase-8" both fire.
    "Phase 9" also fires. Total 2+ F3 findings; len==1 FAILS. RED gate for
    F3-04.
  </behavior>

  <acceptance_criteria>
    - tests/fixtures/f3_phase10_cross_ref.md: no YAML frontmatter, body
      contains "10-02" not inside an ISO date, no audit-notes section
    - tests/fixtures/f3_phase10_own_id_fp.md: YAML frontmatter with
      "phase: 10" and "plan: 2", body contains "10-02", no audit-notes
    - tests/fixtures/f3_phase_text_fp.md: YAML frontmatter with "phase: 8"
      (no plan: field), body contains "Phase 8", "Phase-8", and "Phase 9",
      no references or audit-notes section
    - test_f3_phase10_cross_ref_detected FAILS against unmodified source
      (0 findings returned, assertion len==1 fails)
    - test_f3_phase10_own_id_suppressed PASSES against unmodified source
      (0 findings returned, regression guard confirmed GREEN)
    - test_f3_phase_text_self_ref_suppressed FAILS against unmodified source
      (2+ findings, assertion len==1 fails)
    - All 10 pre-existing F3 tests still pass (regression floor preserved)
    - python3 -m py_compile tests/unit/test_f3_cross_plan_invariant.py: clean
    - ruff check tests/unit/test_f3_cross_plan_invariant.py: clean
    - Non-ASCII check on diff output: no matches
    - AI-smell grep on diff output: no matches
  </acceptance_criteria>

  <action>
    All file edits inside .worktrees/fix-f3-phase10-text-v014/.

    FIXTURE tests/fixtures/f3_phase10_cross_ref.md:
    No YAML frontmatter. Body references "10-02" as a bare plan identifier
    not inside an ISO date context, with no audit-notes section.
    Current _SUBPLAN_RE silently misses "10-02" (false negative).
    After Plan 12-02 extends the regex, returns 1 finding.

    Write exactly:

      # Plan: Database Migration

      ## Overview

      This plan depends on the work completed in plan 10-02.
      The schema changes from 10-02 must be applied before running migrations.

    FIXTURE tests/fixtures/f3_phase10_own_id_fp.md:
    YAML frontmatter with "phase: 10" and "plan: 2". After Plan 12-02,
    _own_id_set() produces "10-02" in own via frontmatter extraction:
    phase_num="10" zfill(2)="10", plan_num="2" zfill(2)="02",
    own.add("10-02"). Body references "10-02". No audit-notes section.

    Write exactly:

      ---
      phase: 10
      plan: 2
      ---

      # 10-02 Schema Migration

      ## Overview

      This is plan 10-02. It performs the database schema migration.
      All tasks in 10-02 must be completed in order.

    FIXTURE tests/fixtures/f3_phase_text_fp.md:
    YAML frontmatter with "phase: 8" only (no plan: field). Body has
    "Phase 8" (matched by _PHASE_RE as "phase 8"), "Phase-8" (matched
    as "phase-8") -- both suppressed after Plan 12-02 D-04 adds phase-text
    tokens to own -- and "Phase 9" which must still fire. No audit-notes.

    Write exactly:

      ---
      phase: 8
      ---

      # Implementation Plan

      ## Overview

      This plan is part of Phase 8. The Phase 8 work covers precision fixes.
      Phase-8 introduced severity downgrade for G8.A.no_citation.

      The work builds on top of Phase 9 improvements which added GSD compatibility.

    TEST STUBS: Append three functions to
    tests/unit/test_f3_cross_plan_invariant.py after the final existing
    function test_f3_own_id_suppressed_via_frontmatter:

      def test_f3_phase10_cross_ref_detected():
          """Phase 10+ subplan ID (10-02) in body must fire one F3 HIGH finding."""
          parsed = parse(_load("f3_phase10_cross_ref.md"))
          findings = f3_cross_plan_invariant.check(parsed)
          assert len(findings) == 1, (
              f"Expected 1 finding for 10-02, got {len(findings)}: {findings}"
          )
          assert "10-02" in findings[0].message, (
              f"Finding message should mention '10-02', got: {findings[0].message}"
          )


      def test_f3_phase10_own_id_suppressed():
          """Phase 10 own-ID (10-02 from frontmatter) must suppress self-references."""
          parsed = parse(_load("f3_phase10_own_id_fp.md"))
          findings = f3_cross_plan_invariant.check(parsed)
          assert findings == [], (
              f"Own id '10-02' from frontmatter should be exempt, got: {findings}"
          )


      def test_f3_phase_text_self_ref_suppressed():
          """Phase-text self-reference must not fire F3; different phase must fire.

          A plan with phase:8 frontmatter mentioning 'Phase 8' and 'Phase-8'
          must produce zero F3 findings for those phrases. A mention of 'Phase 9'
          (different phase, no audit note) must still produce one F3 finding.
          """
          parsed = parse(_load("f3_phase_text_fp.md"))
          findings = f3_cross_plan_invariant.check(parsed)
          f3_findings = [
              f for f in findings
              if f.check_id == "F3.unverified_cross_plan_ref"
          ]
          assert len(f3_findings) == 1, (
              f"Expected exactly 1 F3 finding (Phase 9 only), got {len(f3_findings)}: "
              f"{[f.message for f in f3_findings]}"
          )
          assert any("Phase 9" in f.message for f in f3_findings), (
              f"Remaining finding should mention 'Phase 9', got: "
              f"{[f.message for f in f3_findings]}"
          )
          assert all("Phase 8" not in f.message for f in f3_findings), (
              f"'Phase 8' should be suppressed as own-phase self-reference, got: "
              f"{[f.message for f in f3_findings]}"
          )

    Step 0 pre-check (run from inside worktree root):

      python3 -m py_compile tests/unit/test_f3_cross_plan_invariant.py

      ruff check tests/unit/test_f3_cross_plan_invariant.py

      git diff --diff-filter=AM -U0 | grep '^+' | \
        grep -P '[^\x00-\x7F]' && echo "FAIL: non-ASCII" || echo "OK"

      git diff --diff-filter=AM -U0 | grep '^+' | \
        grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]|P[0-9]+:|S[0-9]+:' && \
        echo "FAIL: AI smell" || echo "OK"

    Confirm RED state:

      uv run pytest tests/unit/test_f3_cross_plan_invariant.py::test_f3_phase10_cross_ref_detected -xvs
      (Expected: FAILED -- 0 findings returned, len==1 assertion fails)

      uv run pytest tests/unit/test_f3_cross_plan_invariant.py::test_f3_phase10_own_id_suppressed -xvs
      (Expected: PASSED -- regression guard is GREEN)

      uv run pytest tests/unit/test_f3_cross_plan_invariant.py::test_f3_phase_text_self_ref_suppressed -xvs
      (Expected: FAILED -- 2+ findings, len==1 assertion fails)

    Run full F3 test file to confirm regression floor:

      uv run pytest tests/unit/test_f3_cross_plan_invariant.py -q

    Expected: 11 passed, 2 failed (13 total).
  </action>

  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/fix-f3-phase10-text-v014 && \
      uv run pytest tests/unit/test_f3_cross_plan_invariant.py -q 2>&1 | tail -10
    </automated>
  </verify>

  <done>
    Three fixture files created. test_f3_phase10_cross_ref_detected and
    test_f3_phase_text_self_ref_suppressed both FAIL against unmodified source
    (RED confirmed). test_f3_phase10_own_id_suppressed PASSES as regression guard.
    All 10 pre-existing F3 tests still pass. Step 0 clean on all new/modified files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture markdown -> parser | Fixed-content .md files; no user input at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-SC | Tampering | no new packages installed in this plan | accept | no package manager installs; legitimacy gate not applicable |
</threat_model>

<verification>
From inside .worktrees/fix-f3-phase10-text-v014/:

  uv run pytest tests/unit/test_f3_cross_plan_invariant.py -q

Expected: 11 passed, 2 failed.
- PASSED: 10 pre-existing + test_f3_phase10_own_id_suppressed (regression guard)
- FAILED: test_f3_phase10_cross_ref_detected (0 findings, expected 1)
- FAILED: test_f3_phase_text_self_ref_suppressed (2+ findings, expected exactly 1)
</verification>

<success_criteria>
1. git worktree list shows .worktrees/fix-f3-phase10-text-v014 at fix/f3-phase10-text-v014
2. tests/fixtures/f3_phase10_cross_ref.md exists (no frontmatter, bare "10-02" in body)
3. tests/fixtures/f3_phase10_own_id_fp.md exists (phase:10 plan:2 frontmatter, "10-02" in body)
4. tests/fixtures/f3_phase_text_fp.md exists (phase:8 frontmatter, "Phase 8", "Phase-8", "Phase 9")
5. test_f3_phase10_cross_ref_detected: FAILED (0 findings returned, expected 1)
6. test_f3_phase10_own_id_suppressed: PASSED (0 findings, regression guard confirmed)
7. test_f3_phase_text_self_ref_suppressed: FAILED (2+ findings, expected exactly 1)
8. All 10 pre-existing F3 tests: PASSED
9. Step 0 clean on all modified/new files
10. Main worktree git status: clean (no modifications)
</success_criteria>

<output>
Create .planning/phases/12-f3-phase10-and-phase-text-suppression/12-01-SUMMARY.md when done.
</output>
