---
phase: 13
plan: 3
type: execute
wave: 3
depends_on:
  - 13-02
files_modified:
  - pyproject.toml
  - src/plan_forge/__init__.py
autonomous: false
requirements:
  - PG-01
  - PG-02
  - PG-03

must_haves:
  truths:
    - "Forge review sub-session runs and reaches zero findings after 3 clean cycles"
    - "Version string in pyproject.toml reads 0.1.5"
    - "Version string in src/plan_forge/__init__.py reads 0.1.5"
    - "Branch fix/f-gate-plan-quality-v015 is merged into main"
    - "Worktree .worktrees/f-gate-plan-quality-v015 is removed"
    - "ROADMAP.md Phase 13 row updated to Complete"
  artifacts:
    - path: "pyproject.toml"
      provides: "Version 0.1.5"
      contains: "0.1.5"
    - path: "src/plan_forge/__init__.py"
      provides: "Version 0.1.5"
      contains: "0.1.5"
  key_links:
    - from: "pyproject.toml"
      to: "src/plan_forge/__init__.py"
      via: "version string 0.1.5 must match in both files"
      pattern: "0\\.1\\.5"
---

<objective>
Run forge review in a separate sub-session; bump version to 0.1.5; merge to
main; clean up worktree; update ROADMAP.

Purpose: Gate the implementation behind a full forge review (3-cycle static
review, zero findings) before shipping. Version bump records the milestone.
Output: v0.1.5 shipped on main; worktree removed; ROADMAP updated.
</objective>

<execution_context>
@/home/houminxi/code/plan-forge/.planning/phases/13-plan-gate-mechanical-checks/13-CONTEXT.md
</execution_context>

<context>
@/home/houminxi/code/plan-forge/.planning/ROADMAP.md
@/home/houminxi/code/plan-forge/.planning/phases/13-plan-gate-mechanical-checks/13-02-SUMMARY.md

<interfaces>
Version string locations (confirmed from RESEARCH.md):
  pyproject.toml: version = "0.1.4"
  src/plan_forge/__init__.py: __version__ = "0.1.4"
  Both must change to "0.1.5".

ROADMAP.md Phase 13 row (to update):
  Progress table: change TBD -> 3/3, In Progress -> Complete, -- -> 2026-05-31
  Traceability table: add PG-01, PG-02, PG-03 rows marked Complete

Forge review requirement (CLAUDE.md):
  Implementation agent and review agent must be separate sub-sessions.
  3-cycle static review: qodo-review, code-review-expert, adversarial-qe
  (9 passes minimum). Zero findings to exit. Step 4 smoke test after clean.

Merge strategy (fast-forward only):
  git -C /home/houminxi/code/plan-forge merge --ff-only fix/f-gate-plan-quality-v015

Worktree removal after merge:
  git -C /home/houminxi/code/plan-forge worktree remove .worktrees/f-gate-plan-quality-v015

Commit convention (CLAUDE.md): write WHY not WHAT; no task IDs; no model names;
no Co-Authored-By lines. Use # post-review-c3 marker only as internal gate,
not in the message body. Non-code commits use # docs or # chore marker.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Run forge review in separate sub-session</name>
  <what-built>The implementation from Plan 13-02 is complete and committed on
  branch fix/f-gate-plan-quality-v015. A separate review sub-session must run
  the forge review pipeline on the six new gate files per CLAUDE.md policy
  (implementation agent and review agent must be separate sub-sessions).</what-built>
  <how-to-verify>
    Launch a new Claude sub-session with the worktree as working directory:

      cd /home/houminxi/code/plan-forge/.worktrees/f-gate-plan-quality-v015

    In that sub-session, run /forge on:
      src/plan_forge/checks/mechanical/__init__.py
      src/plan_forge/checks/mechanical/f10_noun_phrase_duplicate.py
      src/plan_forge/checks/mechanical/f11_ac_test_traceability.py
      src/plan_forge/checks/mechanical/f12_temporal_anchor_lint.py
      src/plan_forge/checks/mechanical/f13_cross_plan_claim_verifier.py
      src/plan_forge/checks/mechanical/f14_task_without_ac.py
      src/plan_forge/checks/mechanical/f15_read_first_validity.py

    The review sub-session must:
    1. Run Step 0: ruff, non-ASCII grep, py_compile on each file
    2. Run 3 full cycles (qodo-review, code-review-expert, adversarial-qe)
    3. Fix all findings; repeat until a full cycle produces zero findings
    4. Run Step 4 smoke test: uv run pytest tests/ -q (all tests pass)

    The review sub-session must NOT commit changes itself. Only the main
    session commits after review is confirmed clean (# post-review-c3 marker).

    After zero findings and smoke test pass, return here and resume.
  </how-to-verify>
  <resume-signal>Type "review clean" after the review sub-session reaches
  zero findings across a complete 3-pass cycle and smoke test passes.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Version bump 0.1.4 to 0.1.5 and commit</name>
  <files>
    pyproject.toml
    src/plan_forge/__init__.py
  </files>
  <action>
Both edits go in the worktree at:
  /home/houminxi/code/plan-forge/.worktrees/f-gate-plan-quality-v015/

In pyproject.toml: change
  version = "0.1.4"
to
  version = "0.1.5"

In src/plan_forge/__init__.py: change
  __version__ = "0.1.4"
to
  __version__ = "0.1.5"

Step 0 after edits:
  python3 -m py_compile src/plan_forge/__init__.py
  python3 -c "open('src/plan_forge/__init__.py').read().encode('ascii')"
  uv run ruff check src/plan_forge/__init__.py

Run full suite:
  uv run pytest tests/ -q 2>&1 | tail -5
  Expected: zero failures.

AI-smell check:
  git diff --diff-filter=AM -U0 | grep '^+' | grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]'
  Expected: no output.

Commit using the # post-review-c3 internal gate marker. The marker goes in the
commit command as an inline comment, NOT in the commit message body:

  git add pyproject.toml src/plan_forge/__init__.py
  git commit -m "release: bump version to 0.1.5

  Ships six new mechanical gate checks for plan quality analysis.

  Signed-off-by: Minxi Hou <houminxi@gmail.com>"  # post-review-c3
  </action>
  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/f-gate-plan-quality-v015 &&
      python3 -c "
import re, pathlib
toml = pathlib.Path('pyproject.toml').read_text()
init = pathlib.Path('src/plan_forge/__init__.py').read_text()
tm = re.search(r'version\s*=\s*\"([^\"]+)\"', toml)
im = re.search(r'__version__\s*=\s*\"([^\"]+)\"', init)
tv = tm.group(1) if tm else 'NOT FOUND'
iv = im.group(1) if im else 'NOT FOUND'
print(f'pyproject.toml: {tv}')
print(f'__init__.py: {iv}')
print('STATUS:', 'PASS' if tv == '0.1.5' and iv == '0.1.5' else 'FAIL')
"
    </automated>
    Expected: both show 0.1.5, STATUS: PASS.
  </verify>
  <done>
    Both files read 0.1.5. Full suite passes. Committed with # post-review-c3 marker.
  </done>
</task>

<task type="auto">
  <name>Task 3: Merge to main, remove worktree, update ROADMAP</name>
  <files>
    .planning/ROADMAP.md
  </files>
  <action>
Run from the MAIN worktree root: /home/houminxi/code/plan-forge/

Step 1 -- merge (fast-forward only):
  git -C /home/houminxi/code/plan-forge merge --ff-only fix/f-gate-plan-quality-v015

If merge fails with "fatal: Not possible to fast-forward", rebase first:
  git -C /home/houminxi/code/plan-forge/.worktrees/f-gate-plan-quality-v015 rebase origin/main
  Then retry the ff merge.

Step 2 -- verify merge:
  git -C /home/houminxi/code/plan-forge log --oneline -5
  Expected: version bump commit at top.

Step 3 -- full suite on main:
  uv run pytest tests/ -q 2>&1 | tail -5
  Expected: zero failures.

Step 4 -- remove worktree:
  git -C /home/houminxi/code/plan-forge worktree remove .worktrees/f-gate-plan-quality-v015

Step 5 -- update ROADMAP.md:
  The check_worktree.sh hook blocks direct Write/Edit tool calls to the main
  worktree. Use a temporary docs worktree to edit ROADMAP.md:

    git -C /home/houminxi/code/plan-forge worktree add .worktrees/docs-13 main
    (edit .worktrees/docs-13/.planning/ROADMAP.md with the changes below)
    cd /home/houminxi/code/plan-forge/.worktrees/docs-13
    git add .planning/ROADMAP.md
    git commit -m "docs: mark Phase 13 complete in roadmap  # docs"
    cd /home/houminxi/code/plan-forge
    git merge --ff-only docs-13
    git worktree remove .worktrees/docs-13

  ROADMAP.md changes:
  a) In the Progress table, update the Phase 13 row:
     Change "TBD" to "3/3", "In Progress" to "Complete", "--" to "2026-05-31"
  b) In the Milestones section add:
     - **v0.1.5 Plan Quality Gate (Mechanical Checks)** - Phase 13 (shipped 2026-05-31)
  c) In the Traceability table add three rows:
     | PG-01 | Phase 13 | Complete |
     | PG-02 | Phase 13 | Complete |
     | PG-03 | Phase 13 | Complete |
  </action>
  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge &&
      python3 -c "
import subprocess
r = subprocess.run(['git', 'log', '--oneline', '-3'], capture_output=True, text=True)
print('git log:', r.stdout.strip())
import pathlib
wt = pathlib.Path('.worktrees/f-gate-plan-quality-v015')
print('worktree removed:', not wt.exists())
r2 = subprocess.run(['uv', 'run', 'pytest', 'tests/', '-q', '--tb=no'],
                    capture_output=True, text=True)
lines = (r2.stdout + r2.stderr).strip().split('\n')
print('pytest:', lines[-1] if lines else 'no output')
roadmap = pathlib.Path('.planning/ROADMAP.md').read_text()
print('ROADMAP PG-01:', 'PG-01' in roadmap and 'Complete' in roadmap)
"
    </automated>
    Expected: log shows version bump at top; worktree removed: True; pytest summary
    shows all passing; ROADMAP PG-01: True.
  </verify>
  <done>
    Branch merged. Worktree removed. ROADMAP updated with Phase 13 Complete and
    PG-01/PG-02/PG-03 traceability rows. Full suite passes on main.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| forge review sub-session | Separate session; reads source only; must not self-commit |
| git merge | Fast-forward only to prevent history rewrite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering | version string consistency | mitigate | verify step checks both pyproject.toml and __init__.py match 0.1.5 |
| T-13-07 | Tampering | merge without review | mitigate | checkpoint:human-action gates on forge review before Task 2 runs |
| T-13-SC | Tampering | package installs | accept | no new packages in this plan |
</threat_model>

<verification>
Final Phase 13 completion check (run from main worktree):

  cd /home/houminxi/code/plan-forge
  git log --oneline -5
  uv run pytest tests/ -q 2>&1 | tail -5
  grep "0\.1\.5" pyproject.toml src/plan_forge/__init__.py
  grep "PG-01\|PG-02\|PG-03" .planning/ROADMAP.md

Expected:
  - git log: version bump commit at top
  - pytest: all pass, zero failures
  - grep 0.1.5: matches in both files
  - grep PG: three traceability rows present
</verification>

<success_criteria>
1. Forge review reaches zero findings across a complete 3-pass cycle (Task 1)
2. Smoke test passes after review: uv run pytest tests/ -q zero failures
3. pyproject.toml version = 0.1.5
4. src/plan_forge/__init__.py __version__ = "0.1.5"
5. Branch fix/f-gate-plan-quality-v015 merged to main via fast-forward
6. Worktree .worktrees/f-gate-plan-quality-v015 removed
7. ROADMAP.md Phase 13 row: 3/3 Complete 2026-05-31
8. ROADMAP.md Traceability: PG-01, PG-02, PG-03 all marked Complete
</success_criteria>

<output>
Create /home/houminxi/code/plan-forge/.planning/phases/13-plan-gate-mechanical-checks/13-03-SUMMARY.md
when this plan is complete.
</output>
