---
phase: 13
plan: 2
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/plan_forge/checks/mechanical/__init__.py
  - src/plan_forge/checks/mechanical/f10_noun_phrase_duplicate.py
  - src/plan_forge/checks/mechanical/f11_ac_test_traceability.py
  - src/plan_forge/checks/mechanical/f12_temporal_anchor_lint.py
  - src/plan_forge/checks/mechanical/f13_cross_plan_claim_verifier.py
  - src/plan_forge/checks/mechanical/f14_task_without_ac.py
  - src/plan_forge/checks/mechanical/f15_read_first_validity.py
autonomous: true
requirements:
  - PG-01
  - PG-02
  - PG-03

must_haves:
  truths:
    - "uv run pytest on all 6 test files passes: 18 passed, 0 failed"
    - "uv run pytest tests/ -q passes (full suite, no regressions)"
    - "__init__.py imports f10 through f15 and calls findings.extend() for each"
    - "Bite test for each gate: temporarily break gate logic, test fails; restore, test passes"
    - "Step 0 passes for all 6 gate files: ruff, non-ASCII, py_compile"
  artifacts:
    - path: "src/plan_forge/checks/mechanical/__init__.py"
      provides: "Updated gate registry with f10-f15 imports and extend() calls"
      contains: "f10_noun_phrase_duplicate"
    - path: "src/plan_forge/checks/mechanical/f10_noun_phrase_duplicate.py"
      provides: "F10 full implementation"
      exports: ["check", "_extract_noun_phrases", "_STOPWORDS"]
    - path: "src/plan_forge/checks/mechanical/f11_ac_test_traceability.py"
      provides: "F11 full implementation"
      exports: ["check", "_test_function_exists"]
    - path: "src/plan_forge/checks/mechanical/f12_temporal_anchor_lint.py"
      provides: "F12 full implementation"
      exports: ["check"]
    - path: "src/plan_forge/checks/mechanical/f13_cross_plan_claim_verifier.py"
      provides: "F13 full implementation"
      exports: ["check", "_prose_word_count"]
    - path: "src/plan_forge/checks/mechanical/f14_task_without_ac.py"
      provides: "F14 full implementation"
      exports: ["check", "_has_ac_or_verify"]
    - path: "src/plan_forge/checks/mechanical/f15_read_first_validity.py"
      provides: "F15 full implementation"
      exports: ["check", "_extract_files_modified", "_is_glob_pattern"]
  key_links:
    - from: "src/plan_forge/checks/mechanical/__init__.py"
      to: "src/plan_forge/checks/mechanical/f10_noun_phrase_duplicate.py"
      via: "from . import f10_noun_phrase_duplicate"
      pattern: "from \\. import f1[0-5]"
    - from: "src/plan_forge/checks/mechanical/f13_cross_plan_claim_verifier.py"
      to: "src/plan_forge/checks/mechanical/f3_cross_plan_invariant.py"
      via: "from plan_forge.checks.mechanical.f3_cross_plan_invariant import _PHASE_RE, _SUBPLAN_RE, _TASK_RE, _heading_is_exempt, _own_id_set"
      pattern: "_PHASE_RE.*f3_cross_plan"
    - from: "src/plan_forge/checks/mechanical/f11_ac_test_traceability.py"
      to: "tests/"
      via: "Path(__file__).resolve().parent.parent.parent.parent.parent / 'tests'"
      pattern: "__file__.*parents.*tests"
---

<objective>
Implement all six gates (F10-F15) with full logic; register them in __init__.py;
confirm GREEN state; run bite tests for each gate.

Purpose: Transform the RED test suite from Plan 13-01 to GREEN. Each gate
produces the correct findings on TP fixtures and no findings on FP fixtures.
The bite test for each gate provides load-bearing evidence that the logic is
not a no-op.

Output: 7 modified files; full test suite GREEN; bite test log confirming each
guard is load-bearing.
</objective>

<execution_context>
@/home/houminxi/code/plan-forge/.planning/phases/13-plan-gate-mechanical-checks/13-CONTEXT.md
@/home/houminxi/code/plan-forge/.planning/phases/13-plan-gate-mechanical-checks/13-RESEARCH.md
</execution_context>

<context>
@/home/houminxi/code/plan-forge/.planning/ROADMAP.md
@/home/houminxi/code/plan-forge/.planning/phases/13-plan-gate-mechanical-checks/13-01-SUMMARY.md

<interfaces>
From src/plan_forge/checks/mechanical/__init__.py (confirmed structure):
  from . import (
      f1_sc_traceability, f2_duplicate_fact, f3_cross_plan_invariant,
      f4_temporal_anchor, f5_r_tag_pruner, f6_preamble_body,
      f7_ascii, f8_plan_consistency, f9_plan_length,
  )
  def run(parsed: ParsedPlan, preamble: str | None = None) -> list[Finding]:
      findings: list[Finding] = []
      findings.extend(f1_sc_traceability.check(parsed))
      ...
      findings.extend(f9_plan_length.check(parsed))
      findings.extend(pbr.run(parsed))
      return findings

F3 internal symbols importable by F13 (confirmed from f3_cross_plan_invariant.py):
  _PHASE_RE     = re.compile(r'\bphase[-\s]\d+\b', re.IGNORECASE)
  _SUBPLAN_RE   = re.compile(r'(?<!\d-)\b(?:0[1-9]|[1-9]\d)-\d{2}\b')
  _TASK_RE      = re.compile(r'\bT\d{2,3}\b')
  _heading_is_exempt(heading: str) -> bool
  _own_id_set(parsed: ParsedPlan) -> set[str]

ParsedPlan fields (no frontmatter attribute -- parse raw_text directly):
  raw_text: str
  sections: dict[str, ParsedSection]  -- ParsedSection has .body: str

ParsedSection heading lines: heading text is the dict key, NOT in section.body.
  Verified: _extract_sections never appends heading lines to body_lines.
  F10 does not need to filter # lines from section bodies.

Repo root from gate file (5 parent() calls from mechanical/):
  _REPO_ROOT = Path(__file__).resolve().parent.parent.parent.parent.parent
  _TESTS_DIR = _REPO_ROOT / "tests"
  If _TESTS_DIR does not exist: gate returns [] silently.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register F10-F15 in __init__.py and implement F10, F14, F15</name>
  <files>
    src/plan_forge/checks/mechanical/__init__.py
    src/plan_forge/checks/mechanical/f10_noun_phrase_duplicate.py
    src/plan_forge/checks/mechanical/f14_task_without_ac.py
    src/plan_forge/checks/mechanical/f15_read_first_validity.py
  </files>
  <action>
All edits in this task go under:
  /home/houminxi/code/plan-forge/.worktrees/f-gate-plan-quality-v015/

--- __init__.py EDIT ---

Read the current file, then add six lines to the import block and six lines
to the run() body. The import block becomes:

  from . import (
      f1_sc_traceability,
      f2_duplicate_fact,
      f3_cross_plan_invariant,
      f4_temporal_anchor,
      f5_r_tag_pruner,
      f6_preamble_body,
      f7_ascii,
      f8_plan_consistency,
      f9_plan_length,
      f10_noun_phrase_duplicate,
      f11_ac_test_traceability,
      f12_temporal_anchor_lint,
      f13_cross_plan_claim_verifier,
      f14_task_without_ac,
      f15_read_first_validity,
  )

Inside run(), after the existing findings.extend(f9_plan_length.check(parsed))
line and before the findings.extend(pbr.run(parsed)) line, add:

  findings.extend(f10_noun_phrase_duplicate.check(parsed))
  findings.extend(f11_ac_test_traceability.check(parsed))
  findings.extend(f12_temporal_anchor_lint.check(parsed))
  findings.extend(f13_cross_plan_claim_verifier.check(parsed))
  findings.extend(f14_task_without_ac.check(parsed))
  findings.extend(f15_read_first_validity.check(parsed))

The docstring update in run(): change "F1-F9" to "F1-F15".

--- f10_noun_phrase_duplicate.py IMPLEMENTATION ---

Module-level constants:

  _STOPWORDS = frozenset({
      "a", "an", "the", "and", "or", "but", "in", "on", "at", "to",
      "for", "of", "with", "by", "from", "as", "is", "are", "was",
      "were", "be", "been", "being", "have", "has", "had", "do",
      "does", "did", "will", "would", "could", "should", "may",
      "might", "must", "shall", "can", "this", "that", "these",
      "those", "it", "its", "not", "no", "so", "if", "then",
  })

  _FINDING_CAP = 10

Helper _extract_noun_phrases(body: str) -> set[str]:
  Use fence-width-aware state machine to skip code blocks (same pattern as
  f4_temporal_anchor.py and parser.py _extract_hedge_words):
    in_code = False, fence_width = ""
    for each line: match r'^(`{3,})'; if fence open/close, toggle in_code; skip line.
  Collect clean_lines (non-fence, non-code lines).
  Tokenize: re.findall(r'\b[a-z][a-z0-9_-]*\b', " ".join(clean_lines).lower())
  Filter tokens: remove those in _STOPWORDS.
  Sliding window of width 3 and 4:
    for i in range(len(tokens)):
      for width in (3, 4):
        if i + width <= len(tokens):
          phrase = " ".join(tokens[i:i+width])
          if any(len(w) >= 4 for w in phrase.split()):
            phrases.add(phrase)
  Return phrases as a set.

check(parsed, **kwargs) -> list[Finding]:
  occurrences: dict[str, set[str]] -- phrase -> set of section headings
  for heading, section in parsed.sections.items():
    for phrase in _extract_noun_phrases(section.body):
      occurrences.setdefault(phrase, set()).add(heading)
  flagged = [(phrase, headings) for phrase, headings in occurrences.items()
             if len(headings) >= 3]
  flagged.sort(key=lambda x: (-len(x[1]), x[0]))
  findings = []
  for phrase, headings in flagged[:_FINDING_CAP]:
    findings.append(Finding(
      check_id="F10.noun_phrase_duplicate",
      severity=Severity.LOW,
      location="line:0",
      message=f"phrase {phrase!r} appears in {len(headings)} sections: {sorted(headings)!r}",
      fix_hint=f"declare {phrase!r} once canonically and reference it elsewhere",
    ))
  return findings

--- f14_task_without_ac.py IMPLEMENTATION ---

Module-level patterns:
  _TASK_FULL_RE = re.compile(r'<task\b([^>]*)>(.*?)</task>', re.DOTALL)
  _TYPE_ATTR_RE = re.compile(r'\btype=["\']([^"\']*)["\']')

Helper _has_ac_or_verify(task_body: str) -> bool:
  return bool(
    re.search(r'<acceptance_criteria\b', task_body)
    or re.search(r'<verify\b', task_body)
  )

check(parsed, **kwargs) -> list[Finding]:
  findings = []
  for m in _TASK_FULL_RE.finditer(parsed.raw_text):
    attrs = m.group(1)
    body = m.group(2)
    type_match = _TYPE_ATTR_RE.search(attrs)
    type_val = type_match.group(1) if type_match else ""
    if type_val.startswith("checkpoint:"):
      continue
    if _has_ac_or_verify(body):
      continue
    lineno = parsed.raw_text[:m.start()].count('\n') + 1
    findings.append(Finding(
      check_id="F14.task_without_ac",
      severity=Severity.HIGH,
      location=f"line:{lineno}",
      message="task has no <acceptance_criteria> or <verify> block",
      fix_hint="add an <acceptance_criteria> block with verifiable exit conditions",
    ))
  return findings

--- f15_read_first_validity.py IMPLEMENTATION ---

Module-level:
  from pathlib import Path
  _REPO_ROOT = Path(__file__).resolve().parent.parent.parent.parent.parent
  _READ_FIRST_RE = re.compile(r'<read_first>(.*?)</read_first>', re.DOTALL)
  _PATH_LINE_RE = re.compile(r'^\s*-\s+(\S+)', re.MULTILINE)

Helper _is_glob_pattern(path_str: str) -> bool:
  return any(c in path_str for c in ('*', '{', '}'))

Helper _extract_files_modified(raw_text: str) -> set[str]:
  Parse YAML frontmatter from raw_text, following the same pattern as
  f3_cross_plan_invariant._own_id_set():
  lines = raw_text.splitlines()
  if not lines or lines[0].strip().lstrip(chr(0xFEFF)) != '---': return set()
  result: set[str] = set()
  collecting = False
  for line in lines[1:]:
    stripped = line.strip()
    if stripped == '---': break
    if stripped.startswith('files_modified:'):
      collecting = True
      continue
    if collecting:
      m = re.match(r'^\s{2,}-\s+(.+)', line)
      if m:
        result.add(m.group(1).strip())
      elif stripped and not stripped.startswith((' ', '\t', '-')):
        collecting = False
  return result

check(parsed, **kwargs) -> list[Finding]:
  files_modified = _extract_files_modified(parsed.raw_text)
  findings = []
  for block_match in _READ_FIRST_RE.finditer(parsed.raw_text):
    block = block_match.group(1)
    for path_match in _PATH_LINE_RE.finditer(block):
      path_str = path_match.group(1).strip()
      if _is_glob_pattern(path_str): continue
      if path_str in files_modified: continue
      p = Path(path_str) if Path(path_str).is_absolute() else _REPO_ROOT / path_str
      if not p.exists():
        lineno = parsed.raw_text[:block_match.start()].count('\n') + 1
        findings.append(Finding(
          check_id="F15.read_first_missing",
          severity=Severity.MEDIUM,
          location=f"line:{lineno}",
          message=f"read_first path not found on disk: {path_str!r}",
          fix_hint="update the path or move it to files_modified if created by this plan",
        ))
  return findings

Step 0 after each file:
  python3 -m py_compile FILE
  python3 -c "open('FILE').read().encode('ascii')"
  uv run ruff check FILE

Bite tests for F10, F14, F15 (run after GREEN for these three):
  F10: In f10_noun_phrase_duplicate.py temporarily change ">= 3" threshold
       to ">= 99"; run pytest test_f10_noun_phrase_duplicate.py::test_f10_fail_fixture
       -- expect FAIL. Restore to ">= 3"; run again -- expect PASS.
  F14: In f14_task_without_ac.py temporarily change "checkpoint:" startswith
       check to "NEVER:"; run pytest test_f14_task_without_ac.py::test_f14_pass_fixture
       -- expect FAIL (checkpoint task now incorrectly fires). Restore; expect PASS.
  F15: In f15_read_first_validity.py temporarily comment out the
       "if path_str in files_modified: continue" guard; run
       pytest test_f15_read_first_validity.py::test_f15_pass_fixture -- expect FAIL.
       Restore; expect PASS.

Record bite test results in 13-02-SUMMARY.md (created in the output section).
  </action>
  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/f-gate-plan-quality-v015 &&
      uv run pytest \
        tests/unit/test_f10_noun_phrase_duplicate.py \
        tests/unit/test_f14_task_without_ac.py \
        tests/unit/test_f15_read_first_validity.py \
        -v 2>&1 | tail -20
    </automated>
    Expected: 9 passed (3 per gate), 0 failed.
    Also verify __init__.py now contains f10-f15 imports:
      grep -c "f10_noun_phrase" src/plan_forge/checks/mechanical/__init__.py
      Expected: 1
  </verify>
  <done>
    9 tests pass for F10, F14, F15. __init__.py imports f10-f15. Bite tests for
    all three gates confirm the guards are load-bearing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement F11, F12, F13 and confirm full GREEN suite</name>
  <files>
    src/plan_forge/checks/mechanical/f11_ac_test_traceability.py
    src/plan_forge/checks/mechanical/f12_temporal_anchor_lint.py
    src/plan_forge/checks/mechanical/f13_cross_plan_claim_verifier.py
  </files>
  <action>
All edits under:
  /home/houminxi/code/plan-forge/.worktrees/f-gate-plan-quality-v015/

--- f11_ac_test_traceability.py IMPLEMENTATION ---

Module-level:
  from pathlib import Path
  _REPO_ROOT = Path(__file__).resolve().parent.parent.parent.parent.parent
  _TESTS_DIR = _REPO_ROOT / "tests"
  _AC_RE = re.compile(r'<acceptance_criteria>(.*?)</acceptance_criteria>', re.DOTALL)
  _TEST_FUNC_RE = re.compile(r'\btest_[a-z_]{4,}\b')
  _PYTEST_COLON_RE = re.compile(r'::(test_[a-z_]{4,})\b')
  _PYTEST_K_RE = re.compile(r'-k\s+(test_[a-z_]{4,})\b')

Helper _test_function_exists(func_name: str, tests_dir: Path) -> bool:
  pattern = re.compile(r'\bdef\s+' + re.escape(func_name) + r'\b')
  for py_file in tests_dir.rglob("*.py"):
    try:
      if pattern.search(py_file.read_text(encoding="utf-8", errors="ignore")):
        return True
    except OSError:
      pass
  return False

check(parsed, **kwargs) -> list[Finding]:
  if not _TESTS_DIR.exists(): return []
  findings = []
  seen: set[str] = set()
  for block_match in _AC_RE.finditer(parsed.raw_text):
    block = block_match.group(1)
    candidates: set[str] = set()
    candidates.update(_PYTEST_COLON_RE.findall(block))
    candidates.update(_PYTEST_K_RE.findall(block))
    candidates.update(_TEST_FUNC_RE.findall(block))
    for func_name in candidates:
      if func_name in seen: continue
      seen.add(func_name)
      if not _test_function_exists(func_name, _TESTS_DIR):
        lineno = parsed.raw_text[:block_match.start()].count('\n') + 1
        findings.append(Finding(
          check_id="F11.ac_test_missing",
          severity=Severity.MEDIUM,
          location=f"line:{lineno}",
          message=f"acceptance_criteria references test function {func_name!r} not found in tests/",
          fix_hint=f"verify the test name is correct or add the test to tests/",
        ))
  return findings

--- f12_temporal_anchor_lint.py IMPLEMENTATION ---

Module-level:
  _AC_RE = re.compile(r'<acceptance_criteria>(.*?)</acceptance_criteria>', re.DOTALL)
  _ACTION_RE = re.compile(r'<action>(.*?)</action>', re.DOTALL)
  _TEMPORAL_ACTION_RE = re.compile(
    r'\b(before|after|once|when|until)\b'
    r'(?:(?!\n).){0,60}'
    r'\b(commit|merge|run|execute|complete|finish|pass|fail)\b',
    re.IGNORECASE,
  )
  _ANCHOR_SIGNALS = (
    re.compile(r'`'),
    re.compile(r'\buv\s+run\b'),
    re.compile(r'\b[a-z]+_[a-z]'),
    re.compile(r'\bgit\s+\w+'),
  )

check(parsed, **kwargs) -> list[Finding]:
  findings = []
  blocks: list[tuple[str, int]] = []  -- (block_text, lineno_of_block_start)
  for m in _AC_RE.finditer(parsed.raw_text):
    blocks.append((m.group(1), parsed.raw_text[:m.start()].count('\n') + 1))
  for m in _ACTION_RE.finditer(parsed.raw_text):
    blocks.append((m.group(1), parsed.raw_text[:m.start()].count('\n') + 1))
  for block_text, block_lineno in blocks:
    for line in block_text.splitlines():
      if not _TEMPORAL_ACTION_RE.search(line): continue
      if any(sig.search(line) for sig in _ANCHOR_SIGNALS): continue
      findings.append(Finding(
        check_id="F12.temporal_anchor_unanchored",
        severity=Severity.LOW,
        location=f"line:{block_lineno}",
        message=f"temporal phrase lacks a concrete anchor: {line.strip()!r}",
        fix_hint="anchor the phrase with a backtick command, snake_case identifier, or git command",
      ))
  return findings

--- f13_cross_plan_claim_verifier.py IMPLEMENTATION ---

Import direction: F13 imports from F3 only. Never the reverse.

Module-level:
  from plan_forge.checks.mechanical.f3_cross_plan_invariant import (
      _PHASE_RE, _SUBPLAN_RE, _TASK_RE,
      _heading_is_exempt, _own_id_set,
  )

Helper _prose_word_count(sentence: str) -> int:
  Count words in sentence that are NOT cross-plan reference tokens:
  words = sentence.split()
  count = 0
  for word in words:
    clean = word.strip(".,;:()")
    if (_PHASE_RE.fullmatch(clean)
        or _SUBPLAN_RE.fullmatch(clean)
        or _TASK_RE.fullmatch(clean)):
      continue
    count += 1
  return count

check(parsed, **kwargs) -> list[Finding]:
  own = _own_id_set(parsed)
  findings = []
  for heading, section in parsed.sections.items():
    if not _heading_is_exempt(heading): continue
    for pattern in (_PHASE_RE, _SUBPLAN_RE, _TASK_RE):
      for m in pattern.finditer(section.body):
        ref = m.group(0).lower()
        if ref in own: continue
        start = max(0, m.start() - 100)
        end = min(len(section.body), m.end() + 100)
        sentence = section.body[start:end]
        if _prose_word_count(sentence) < 10:
          lineno = parsed.raw_text.find(section.body)
          lineno = parsed.raw_text[:lineno].count('\n') + 1 if lineno >= 0 else 0
          findings.append(Finding(
            check_id="F13.cross_plan_claim_thin",
            severity=Severity.MEDIUM,
            location=f"line:{lineno}",
            message=(
              f"cross-plan reference {m.group(0)!r} in audit/references section"
              f" has fewer than 10 prose words of context"
            ),
            fix_hint="expand the reference entry to explain what the plan contains and why it is relevant",
          ))
  return findings

Step 0 after each file:
  python3 -m py_compile FILE
  python3 -c "open('FILE').read().encode('ascii')"
  uv run ruff check FILE

After all three are GREEN, run full suite:
  uv run pytest tests/ -q 2>&1 | tail -10

Bite tests for F11, F12, F13:
  F11: In f11_ac_test_traceability.py temporarily invert _test_function_exists
       to return True always (def _test_function_exists(...): return True).
       Run pytest test_f11_ac_test_traceability.py::test_f11_fail_fixture -- FAIL.
       Restore; run -- PASS.
  F12: In f12_temporal_anchor_lint.py temporarily clear _ANCHOR_SIGNALS to an
       empty tuple. Run pytest test_f12_temporal_anchor_lint.py::test_f12_pass_fixture
       -- FAIL (anchored FP fixture now fires). Restore; run -- PASS.
  F13: In f13_cross_plan_claim_verifier.py temporarily change "< 10" threshold
       to "< 999". Run pytest test_f13_cross_plan_claim_verifier.py::test_f13_pass_fixture
       -- FAIL (FP fixture now fires). Restore; run -- PASS.

Record bite results in 13-02-SUMMARY.md.

AI-smell check before committing (run from worktree root):
  git diff HEAD --diff-filter=AM -U0 | grep '^+' | grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]|P[0-9]+:|S[0-9]+:'
  Expected: no output (zero matches).

Commit all changes with a human-readable message. No task IDs, no model names,
no Co-Authored-By lines. Example:
  git add src/plan_forge/checks/mechanical/ tests/
  git commit -m "mechanical: add plan quality gates F10-F15

  Six new mechanical checks catch writing-quality issues before AI panel review:
  noun-phrase cross-section duplication, AC-to-test traceability, temporal anchor
  lint, cross-plan claim thinness, missing acceptance criteria, and invalid
  read_first paths.

  Signed-off-by: Minxi Hou <houminxi@gmail.com>"
  </action>
  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/f-gate-plan-quality-v015 &&
      uv run pytest tests/ -q 2>&1 | tail -15
    </automated>
    Expected: full suite passes with zero failures. Summary includes the 18 new
    tests (3 per gate) plus all pre-existing tests. No regressions.

    Also verify AI-smell is absent from the diff:
      git diff HEAD --diff-filter=AM -U0 | grep '^+' | grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]'
    Expected: no output.
  </verify>
  <done>
    Full test suite passes with zero failures. All 6 bite tests confirm guards are
    load-bearing. AI-smell check returns no output. Changes committed on branch
    fix/f-gate-plan-quality-v015.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed.raw_text | Input from plan document under review; no shell execution |
| tests/ filesystem scan (F11, F15) | Read-only rglob + read_text; no writes |
| read_first paths (F15) | Used only in Path.exists(); no execution or open() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Tampering | F15 path existence check | accept | Path.exists() only; no file read or exec |
| T-13-04 | Denial of Service | F11 rglob on large tests/ | accept | tests/ is bounded; no user-controlled glob |
| T-13-05 | Information Disclosure | F15 missing-path message | accept | path strings come from the plan being reviewed, not secrets |
| T-13-SC | Tampering | package installs | accept | zero new packages; stdlib + existing plan_forge deps only |
</threat_model>

<verification>
Full GREEN state and regression check:

  cd /home/houminxi/code/plan-forge/.worktrees/f-gate-plan-quality-v015
  uv run pytest tests/ -q 2>&1 | tail -15

Expected: all tests pass, zero failures.

Verify __init__.py registration:
  grep "f1[0-5]" src/plan_forge/checks/mechanical/__init__.py
  Expected: 6 lines (one import per gate, six extend() calls).

Verify AI-smell absence:
  git diff HEAD --diff-filter=AM -U0 | grep '^+' | grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]'
  Expected: no output.

Verify non-ASCII absence in new gate files:
  for f in src/plan_forge/checks/mechanical/f1[0-5]_*.py; do
    python3 -c "open('$f').read().encode('ascii')" && echo "$f: OK"
  done
</verification>

<success_criteria>
1. uv run pytest tests/ passes with zero failures (all pre-existing + 18 new tests)
2. __init__.py registers F10-F15: 6 new import lines + 6 new extend() calls
3. Each gate file: ruff clean, py_compile clean, ASCII-only
4. Bite test log: each of 6 gates was broken and failed, then restored and passed
5. AI-smell grep returns zero matches on the diff
6. Changes committed on branch fix/f-gate-plan-quality-v015 with human-readable message
</success_criteria>

<output>
Create /home/houminxi/code/plan-forge/.planning/phases/13-plan-gate-mechanical-checks/13-02-SUMMARY.md
when this plan is complete. Include the bite test results table.
</output>
