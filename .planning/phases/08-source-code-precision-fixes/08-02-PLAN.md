---
phase: 08-source-code-precision-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/plan_forge/checks/epistemic/g8_source_diversity.py
autonomous: true
requirements:
  - GATE-01
  - GATE-01T
  - GATE-02
  - GATE-03

must_haves:
  truths:
    - "test_prose_cited_emits_high PASSES (GREEN: prose-cited plan returns HIGH)"
    - "test_empty_body_emits_blocker PASSES (empty body still returns BLOCKER)"
    - "All pre-existing G8 tests still pass (no regression)"
    - "Bug-inject cycle completed: revert to BLOCKER -> prose fixture fires BLOCKER
      -> restore to HIGH -> prose fixture returns HIGH"
    - "Full test suite green (761 baseline + new tests)"
  artifacts:
    - path: "src/plan_forge/checks/epistemic/g8_source_diversity.py"
      provides: "G8.A check with body.strip() branch: empty -> BLOCKER, non-empty -> HIGH"
      contains: "Severity.HIGH"
  key_links:
    - from: "g8_source_diversity.py line ~98"
      to: "Severity.HIGH"
      via: "inline ternary: Severity.BLOCKER if not body.strip() else Severity.HIGH"
      pattern: "not body.strip"
---

<objective>
Apply the G8 severity fix: replace the unconditional BLOCKER in
G8.A.no_citation with a two-branch ternary that emits BLOCKER for a truly
empty External Voices body and HIGH for a non-empty body with no parseable
citations. Run the bug-inject cycle (GATE-01T) before committing.

Purpose: Prose-cited plans ("Nielsen (2022) showed...") were blocked by a
BLOCKER finding they cannot fix (adding a parseable citation would change the
author's writing style). The HIGH severity correctly surfaces the issue for
human review without blocking the plan.

Output: Modified g8_source_diversity.py with GREEN tests for GATE-01 and
GATE-03; GATE-02 anti-gutting confirmed; bug-inject teeth verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/houminxi/code/plan-forge/.planning/phases/08-source-code-precision-fixes/08-CONTEXT.md
@/home/houminxi/code/plan-forge/.planning/phases/08-source-code-precision-fixes/08-RESEARCH.md
@/home/houminxi/code/plan-forge/.planning/phases/08-source-code-precision-fixes/08-01-SUMMARY.md

<interfaces>
From src/plan_forge/checks/epistemic/g8_source_diversity.py (current lines 98-105):

  if len(parsed.citations) == 0:
      findings.append(Finding(
          check_id="G8.A.no_citation",
          severity=Severity.BLOCKER,
          location="External Voices",
          message="no citation found in External Voices section",
          fix_hint="add at least one citation in Author (Year) format",
      ))

After fix (replace the severity= line with a two-branch ternary):

  if len(parsed.citations) == 0:
      severity = (
          Severity.BLOCKER if not body.strip()
          else Severity.HIGH
      )
      findings.append(Finding(
          check_id="G8.A.no_citation",
          severity=severity,
          location="External Voices",
          message="no citation found in External Voices section",
          fix_hint="add at least one citation in Author (Year) format",
      ))

Key facts:
  - body is guaranteed str at line 98 (None branch returns at line 82-93).
  - not body.strip() is True for "" and whitespace-only.
  - Severity.HIGH is defined in verdict.py (confirmed grep).
  - No other code in this function needs changing.
  - body variable is in scope (assigned at line 80).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply G8 severity fix and verify GREEN</name>
  <files>src/plan_forge/checks/epistemic/g8_source_diversity.py</files>

  <read_first>
    - /home/houminxi/code/plan-forge/src/plan_forge/checks/epistemic/g8_source_diversity.py
      lines 75-130 (full Part A block: body lookup at line 80, None-return at
      82-93, citations check at 98-105 -- this is the edit target)
    - /home/houminxi/code/plan-forge/src/plan_forge/verdict.py lines 12-27
      (Severity enum: confirm BLOCKER and HIGH both defined)
    - /home/houminxi/code/plan-forge/.planning/phases/08-source-code-precision-fixes/08-01-SUMMARY.md
      (confirm Plan 01 baseline: exact pre-existing test count)
  </read_first>

  <behavior>
    After the edit:
    - test_prose_cited_emits_high: PASSES (non-empty body -> HIGH)
    - test_empty_body_emits_blocker: PASSES (empty body -> BLOCKER)
    - test_no_external_voices_section_blocker: PASSES (body is None -> no_section
      fires, no_citation not reached -- unchanged behavior)
    - test_missing_dissent_blocker: PASSES (unchanged)
    - test_missing_failure_case_blocker: PASSES (unchanged)
    All Part B tests: PASSES (Part B does not use G8.A.no_citation severity)
  </behavior>

  <action>
    Edit inside .worktrees/fix-precision/ only.

    In src/plan_forge/checks/epistemic/g8_source_diversity.py, locate lines
    98-105 (the G8.A.no_citation block). Replace:

      findings.append(Finding(
          check_id="G8.A.no_citation",
          severity=Severity.BLOCKER,
          location="External Voices",
          message="no citation found in External Voices section",
          fix_hint="add at least one citation in Author (Year) format",
      ))

    With:

      severity = (
          Severity.BLOCKER if not body.strip()
          else Severity.HIGH
      )
      findings.append(Finding(
          check_id="G8.A.no_citation",
          severity=severity,
          location="External Voices",
          message="no citation found in External Voices section",
          fix_hint="add at least one citation in Author (Year) format",
      ))

    The if len(parsed.citations) == 0: guard line stays unchanged. Only the
    Finding() call inside the block changes. The severity variable is introduced
    inline; no helper function needed (per RESEARCH.md dont_hand_roll table).

    Step 0 pre-check on modified source file:
      python3 -m py_compile
        src/plan_forge/checks/epistemic/g8_source_diversity.py
      ruff check src/plan_forge/checks/epistemic/g8_source_diversity.py
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -P '[^\x00-\x7F]' && echo "FAIL: non-ASCII" || echo "OK"
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]|P[0-9]+:|S[0-9]+:' &&
        echo "FAIL: AI smell" || echo "OK"

    Then run G8 tests to confirm GREEN:
      uv run pytest tests/unit/test_g8_source_diversity.py -q
  </action>

  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/fix-precision && uv run pytest
      tests/unit/test_g8_source_diversity.py -q 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    All G8 tests pass including test_prose_cited_emits_high (HIGH) and
    test_empty_body_emits_blocker (BLOCKER). Step 0 clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bug-inject cycle GATE-01T (manual teeth verification)</name>

  <what-built>
    G8 source fix is GREEN. Before committing, the implementer must perform
    the bug-inject cycle to confirm the severity variable is the operative
    guard -- not that G8.A.no_citation is inadvertently disabled.
  </what-built>

  <how-to-verify>
    Perform the following steps from inside .worktrees/fix-precision/:

    STEP A -- Revert severity to BLOCKER (working-tree only, no commit):
    In g8_source_diversity.py, temporarily change the ternary back to
    unconditional BLOCKER:

      severity = Severity.BLOCKER  # temporary bug-inject

    STEP B -- Run test_prose_cited_emits_high with injected bug:
      uv run pytest
        tests/unit/test_g8_source_diversity.py::TestG8PartA::test_prose_cited_emits_high
        -xvs

    Expected: FAILED (AssertionError: BLOCKER != HIGH)
    This confirms the FP fixture fires as BLOCKER under the reverted code.

    STEP C -- Restore the fix:
      git checkout -- src/plan_forge/checks/epistemic/g8_source_diversity.py

    STEP D -- Confirm the fix is back and test passes:
      uv run pytest
        tests/unit/test_g8_source_diversity.py::TestG8PartA::test_prose_cited_emits_high
        -xvs

    Expected: PASSED (HIGH)

    Record the result for the commit message:
      Fixture: g8_prose_cited_fp.md
      Before restore (injected): 1 BLOCKER (FAILED test)
      After restore: 1 HIGH (PASSED test)
  </how-to-verify>

  <resume-signal>
    Type "gate01t-confirmed: [fixture name] BLOCKER before, HIGH after" to
    confirm bug-inject cycle passed, then proceed to commit.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Full suite green and commit G8 fix</name>
  <files>src/plan_forge/checks/epistemic/g8_source_diversity.py</files>

  <read_first>
    - Result of Task 2 (bug-inject cycle outcome -- fixture name and counts)
    - git -C /home/houminxi/code/plan-forge log --oneline -3 (commit style ref)
  </read_first>

  <action>
    From inside .worktrees/fix-precision/, run full suite:
      uv run pytest tests/ -q

    Must pass all 761 baseline tests plus the new G8 tests. If any pre-existing
    test fails that was passing in Plan 01, stop -- do not commit. Diagnose
    the regression before proceeding.

    If green, stage and commit with a human-voice message. The message body
    records the bug-inject result from GATE-01T. No task IDs, no model names,
    no review metadata, no Co-Authored-By AI lines. Example structure:

      checks/g8: downgrade no_citation to HIGH for non-empty External Voices

      A prose-cited plan ("Nielsen (2022) showed...") triggers G8.A.no_citation
      because the parser only captures list-item citations, not inline prose.
      The finding was BLOCKER, which blocked the plan even though a real source
      was cited. Distinguish between a truly empty External Voices body (which
      still warrants BLOCKER -- nothing cited at all) and a non-empty body with
      no parseable list citation (HIGH -- a human should verify the source).

      Bug-inject confirmed: reverting the ternary to unconditional BLOCKER causes
      g8_prose_cited_fp.md to fire BLOCKER and test_prose_cited_emits_high to
      FAIL. Restoring yields HIGH and PASSED.

      Signed-off-by: Minxi Hou <houminxi@gmail.com>

    Note: this commit is on the feature branch fix/precision-v011. It is a
    pre-review commit. Plan 08-04 runs the full forge review pipeline; if the
    review sub-session produces findings, amendment commits may be added to this
    branch before merge.

    Stage only the source file (not test files -- those commit with the F2 fix
    or in a combined commit after Plan 03, depending on workflow preference):
      git add src/plan_forge/checks/epistemic/g8_source_diversity.py

    Or stage all modified files in this plan's scope:
      git add src/plan_forge/checks/epistemic/g8_source_diversity.py
        tests/fixtures/g8_prose_cited_fp.md
        tests/fixtures/g8_empty_body_blocker.md
        tests/unit/test_g8_source_diversity.py

    Note: fixture and test files were created in Plan 01 but can be committed
    here with the source change they validate. Do NOT include f2* files in this
    commit -- those belong to Plan 03's commit.
  </action>

  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/fix-precision &&
      uv run pytest tests/ -q 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    Full suite green. G8 fix committed on fix/precision-v011. Commit message
    is human-voice with bug-inject result recorded. No AI smell in commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed.citations (len) -> Severity branch | Internal Python logic; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | g8_source_diversity.py severity logic | mitigate | bug-inject cycle (GATE-01T) confirms the ternary is load-bearing; regression tests prevent accidental revert |
| T-08-SC | Tampering | no new packages installed | accept | no package manager installs; legitimacy gate not applicable |
</threat_model>

<verification>
From inside .worktrees/fix-precision/:

  uv run pytest tests/unit/test_g8_source_diversity.py -v

All tests pass including:
  TestG8PartA::test_prose_cited_emits_high -- PASSED (HIGH severity)
  TestG8PartA::test_empty_body_emits_blocker -- PASSED (BLOCKER severity)
  TestG8PartA::test_no_external_voices_section_blocker -- PASSED
  TestG8PartA::test_missing_dissent_blocker -- PASSED
  TestG8PartA::test_missing_failure_case_blocker -- PASSED
</verification>

<success_criteria>
1. g8_source_diversity.py lines ~98-107 contain the body.strip() ternary
2. All G8 tests pass (5 existing + 2 new = 7 minimum)
3. Full suite: 761+ pass, 0 new failures
4. Bug-inject cycle GATE-01T confirmed (prose fixture fires BLOCKER when
   reverted, HIGH when restored)
5. Commit on fix/precision-v011 with human-voice message and Signed-off-by
6. Step 0 clean on g8_source_diversity.py diff
</success_criteria>

<output>
Create .planning/phases/08-source-code-precision-fixes/08-02-SUMMARY.md when done.
</output>
