---
phase: 08-source-code-precision-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/plan_forge/checks/mechanical/f2_duplicate_fact.py
autonomous: true
requirements:
  - GATE-04
  - GATE-04T
  - GATE-05

must_haves:
  truths:
    - "test_f2_heading_fp_suppressed PASSES (GREEN: heading-derived n-gram silenced)"
    - "test_f2_genuine_phrase_not_suppressed PASSES (genuine repetition still fires)"
    - "All pre-existing F2 tests still pass (no regression)"
    - "Bug-inject cycle completed: remove heading guard -> heading-term FP fires
      -> restore guard -> silence"
    - "Full test suite green (761 baseline + new tests)"
  artifacts:
    - path: "src/plan_forge/checks/mechanical/f2_duplicate_fact.py"
      provides: "F2 check with heading_words subset guard before the findings loop"
      contains: "heading_words"
  key_links:
    - from: "f2_duplicate_fact.py flagged loop"
      to: "heading_words set"
      via: "ng_words <= heading_words subset test"
      pattern: "heading_words"
---

<objective>
Apply the F2 heading-aware suppression: build a heading_words set from
parsed.sections keys, then skip any flagged n-gram whose words are all
present in heading_words. Run the bug-inject cycle (GATE-04T) before
committing.

Purpose: Plan section headings like "Implementation Tasks" and "Success
Criteria" are repeated across sections by design (navigation anchors). F2
flagged the resulting n-grams as duplicate-fact violations with LOW severity,
creating noise that obscured real issues. The heading-word subset test targets
exactly these structural terms without touching the genuine TP case
("DatabaseConnection Pool") whose words do not appear in any heading.

Output: Modified f2_duplicate_fact.py with GREEN tests for GATE-04 and
GATE-05; bug-inject teeth verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/houminxi/code/plan-forge/.planning/phases/08-source-code-precision-fixes/08-CONTEXT.md
@/home/houminxi/code/plan-forge/.planning/phases/08-source-code-precision-fixes/08-RESEARCH.md
@/home/houminxi/code/plan-forge/.planning/phases/08-source-code-precision-fixes/08-01-SUMMARY.md

<interfaces>
From src/plan_forge/checks/mechanical/f2_duplicate_fact.py (current lines 58-76):

  # Sort: descending count, then alphabetical for tie-breaking
  flagged.sort(key=lambda x: (-len(x[1]), x[0]))

  findings: list[Finding] = []
  for ng, headings in flagged[:_FINDING_CAP]:
      findings.append(Finding(
          check_id="F2.duplicate_fact",
          severity=Severity.LOW,
          location="line:0",
          message=(
              f"phrase {ng!r} appears in {len(headings)} sections:"
              f" {sorted(headings)!r}"
          ),
          fix_hint=(
              f"declare {ng!r} canonically in one section and"
              f" reference it elsewhere"
          ),
      ))

After fix -- insert heading_words build between sort and findings loop:

  flagged.sort(key=lambda x: (-len(x[1]), x[0]))

  heading_words: set[str] = set()
  for heading in parsed.sections:
      heading_words.update(heading.lower().split())

  findings: list[Finding] = []
  for ng, headings in flagged[:_FINDING_CAP]:
      if set(ng.split()) <= heading_words:
          continue
      findings.append(Finding(
          check_id="F2.duplicate_fact",
          severity=Severity.LOW,
          location="line:0",
          message=(
              f"phrase {ng!r} appears in {len(headings)} sections:"
              f" {sorted(headings)!r}"
          ),
          fix_hint=(
              f"declare {ng!r} canonically in one section and"
              f" reference it elsewhere"
          ),
      ))

Key facts:
  - parsed.sections is dict[str, ParsedSection]; iterating the dict yields
    heading strings (keys). Do NOT iterate section.body.
  - heading.lower().split() handles mixed-case headings correctly.
  - ng is already lowercase (from _extract_ngrams); ng.split() needs no
    further lowercasing.
  - set(ng.split()) <= heading_words is the idiomatic subset test (Python set
    operator). Both words must appear in heading_words; one missing = not
    suppressed.
  - _FINDING_CAP = 20 stays unchanged; the continue guard runs before append.
  - findings: list[Finding] = [] stays in place; heading_words build goes
    between the sort line and the findings = [] line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply F2 heading-words guard and verify GREEN</name>
  <files>src/plan_forge/checks/mechanical/f2_duplicate_fact.py</files>

  <read_first>
    - /home/houminxi/code/plan-forge/src/plan_forge/checks/mechanical/f2_duplicate_fact.py
      (full file -- 77 lines; edit target is between line 59 and 61)
    - /home/houminxi/code/plan-forge/tests/fixtures/f2_fail.md
      (existing TP fixture; confirm its headings: "Background", "Design",
      "Implementation", "Testing", "Summary" -- "databaseconnection" and
      "pool" do NOT appear as heading words)
    - /home/houminxi/code/plan-forge/.planning/phases/08-source-code-precision-fixes/08-01-SUMMARY.md
      (confirm Plan 01 baseline: exact test count)
  </read_first>

  <behavior>
    After the edit:
    - test_f2_heading_fp_suppressed: PASSES (heading_words contains both
      "implementation" and "tasks"; set("implementation tasks".split()) <=
      heading_words is True; finding suppressed)
    - test_f2_genuine_phrase_not_suppressed: PASSES (f2_fail.md headings do
      not contain "databaseconnection" or "pool"; subset test is False;
      finding emitted as before)
    - test_f2_pass_fixture: PASSES (no flagged ngrams; guard path not reached)
    - test_f2_fail_fixture: PASSES (genuine FP still fires)
    - test_f2_severity: PASSES (LOW severity unchanged)
    - test_f2_edge_exactly_two_sections: PASSES (below 3-section threshold;
      guard path not reached)
  </behavior>

  <action>
    Edit inside .worktrees/fix-precision/ only.

    In src/plan_forge/checks/mechanical/f2_duplicate_fact.py, between the
    flagged.sort(...) line (line 59) and the findings: list[Finding] = []
    line (line 61), insert the heading_words build:

      heading_words: set[str] = set()
      for heading in parsed.sections:
          heading_words.update(heading.lower().split())

    Then inside the for loop body, before findings.append(...), insert the
    subset guard:

      if set(ng.split()) <= heading_words:
          continue

    Do NOT iterate over section.body words -- only heading strings (dict keys).
    Do NOT lowercase the n-gram side -- it is already lowercase from
    _extract_ngrams. Use Python set <= operator, not a manual loop.

    Step 0 pre-check on modified source file:
      python3 -m py_compile
        src/plan_forge/checks/mechanical/f2_duplicate_fact.py
      ruff check src/plan_forge/checks/mechanical/f2_duplicate_fact.py
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -P '[^\x00-\x7F]' && echo "FAIL: non-ASCII" || echo "OK"
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]|P[0-9]+:|S[0-9]+:' &&
        echo "FAIL: AI smell" || echo "OK"

    Run F2 tests to confirm GREEN:
      uv run pytest tests/unit/test_f2_duplicate_fact.py -q
  </action>

  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/fix-precision && uv run pytest
      tests/unit/test_f2_duplicate_fact.py -q 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    All F2 tests pass including test_f2_heading_fp_suppressed (suppressed) and
    test_f2_genuine_phrase_not_suppressed (fires). Step 0 clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bug-inject cycle GATE-04T (manual teeth verification)</name>

  <what-built>
    F2 source fix is GREEN. Before committing, the implementer must perform
    the bug-inject cycle to confirm the heading-words guard is the operative
    suppression -- not that F2 is inadvertently disabled for all inputs.
  </what-built>

  <how-to-verify>
    Perform the following steps from inside .worktrees/fix-precision/:

    STEP A -- Remove the heading-words guard (working-tree only, no commit):
    In f2_duplicate_fact.py, temporarily comment out or delete the guard block:

      # heading_words: set[str] = set()
      # for heading in parsed.sections:
      #     heading_words.update(heading.lower().split())

    And comment out the continue guard inside the loop:

      # if set(ng.split()) <= heading_words:
      #     continue

    STEP B -- Run test_f2_heading_fp_suppressed with injected bug:
      uv run pytest
        tests/unit/test_f2_duplicate_fact.py::test_f2_heading_fp_suppressed
        -xvs

    Expected: FAILED (AssertionError: findings != [])
    This confirms the structural-term FP fixture fires F2 findings when the
    guard is removed.

    STEP C -- Restore the fix:
      git checkout -- src/plan_forge/checks/mechanical/f2_duplicate_fact.py

    STEP D -- Confirm the fix is back and test passes:
      uv run pytest
        tests/unit/test_f2_duplicate_fact.py::test_f2_heading_fp_suppressed
        -xvs

    Expected: PASSED (silence)

    Also run test_f2_genuine_phrase_not_suppressed to confirm the TP still
    fires after restore:
      uv run pytest
        tests/unit/test_f2_duplicate_fact.py::test_f2_genuine_phrase_not_suppressed
        -xvs
    Expected: PASSED

    Record the result for the commit message:
      Fixture: f2_heading_fp.md
      Before restore (guard removed): 1+ F2 findings for "implementation tasks"
      After restore: 0 findings (suppressed)
      TP (f2_fail.md): still fires after restore (unaffected)
  </how-to-verify>

  <resume-signal>
    Type "gate04t-confirmed: heading-term FP fires when guard removed, silenced
    when restored, TP unaffected" to confirm bug-inject cycle passed, then
    proceed to commit.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Full suite green and commit F2 fix</name>
  <files>src/plan_forge/checks/mechanical/f2_duplicate_fact.py</files>

  <read_first>
    - Result of Task 2 (bug-inject cycle outcome -- finding count before/after)
    - git -C /home/houminxi/code/plan-forge log --oneline -3 (commit style ref)
  </read_first>

  <action>
    From inside .worktrees/fix-precision/, run full suite:
      uv run pytest tests/ -q

    Must pass all 761 baseline tests plus the new F2 tests. If any pre-existing
    test fails that was passing before this plan, stop and diagnose.

    If green, stage and commit with a human-voice message. The message body
    records the bug-inject result from GATE-04T. No task IDs, no model names,
    no review metadata, no Co-Authored-By AI lines. Example structure:

      checks/f2: suppress n-grams whose words appear in section headings

      Section headings like "Implementation Tasks" and "Success Criteria" are
      intentionally repeated across sections as navigation anchors. F2 treated
      the resulting bigrams as duplicate-fact violations, producing LOW findings
      that could not be fixed without restructuring valid plans. Build a set of
      all words that appear in any section heading; skip any flagged n-gram
      whose words are a subset of that set. Genuine factual repetitions
      (e.g. "DatabaseConnection Pool" in a plan with unrelated headings) are
      unaffected because their words do not appear as heading words.

      Bug-inject confirmed: removing the heading-words guard causes
      f2_heading_fp.md to fire F2 findings for "implementation tasks".
      Restoring the guard silences it. The f2_fail.md TP still fires after
      restore (genuine phrase unaffected).

      Signed-off-by: Minxi Hou <houminxi@gmail.com>

    Note: this commit is on the feature branch fix/precision-v011. It is a
    pre-review commit. Plan 08-04 runs the full forge review pipeline; if the
    review sub-session produces findings, amendment commits may be added to this
    branch before merge.

    Stage the source file and the F2-related test artifacts created in Plan 01:
      git add src/plan_forge/checks/mechanical/f2_duplicate_fact.py
        tests/fixtures/f2_heading_fp.md
        tests/unit/test_f2_duplicate_fact.py
  </action>

  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/fix-precision &&
      uv run pytest tests/ -q 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    Full suite green. F2 fix committed on fix/precision-v011. Commit message
    is human-voice with bug-inject result recorded. No AI smell in commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed.sections dict keys -> heading_words set | Internal Python; section keys from parser, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Tampering | f2_duplicate_fact.py suppression logic | mitigate | bug-inject cycle (GATE-04T) confirms guard is load-bearing; TP test (GATE-05) prevents over-suppression |
| T-08-SC | Tampering | no new packages installed | accept | no package manager installs; legitimacy gate not applicable |
</threat_model>

<verification>
From inside .worktrees/fix-precision/:

  uv run pytest tests/unit/test_f2_duplicate_fact.py -v

All tests pass including:
  test_f2_heading_fp_suppressed -- PASSED (no findings for heading-derived n-gram)
  test_f2_genuine_phrase_not_suppressed -- PASSED (genuine phrase still fires)
  test_f2_pass_fixture -- PASSED
  test_f2_fail_fixture -- PASSED
  test_f2_severity -- PASSED
  test_f2_edge_exactly_two_sections -- PASSED
</verification>

<success_criteria>
1. f2_duplicate_fact.py contains heading_words set build and subset guard
2. All F2 tests pass (4 existing + 2 new = 6 minimum)
3. Full suite: 761+ pass, 0 new failures
4. Bug-inject cycle GATE-04T confirmed (heading-term FP fires when guard
   removed; TP unaffected after restore)
5. Commit on fix/precision-v011 with human-voice message and Signed-off-by
6. Step 0 clean on f2_duplicate_fact.py diff
</success_criteria>

<output>
Create .planning/phases/08-source-code-precision-fixes/08-03-SUMMARY.md when done.
</output>
