---
phase: 08-source-code-precision-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .worktrees/fix-precision (worktree created)
  - tests/fixtures/g8_prose_cited_fp.md
  - tests/fixtures/g8_empty_body_blocker.md
  - tests/fixtures/f2_heading_fp.md
  - tests/unit/test_g8_source_diversity.py
  - tests/unit/test_f2_duplicate_fact.py
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
  - GATE-04
  - GATE-05

must_haves:
  truths:
    - "test_prose_cited_emits_high FAILS against unmodified source (RED: BLOCKER != HIGH)"
    - "test_f2_heading_fp_suppressed FAILS against unmodified source (RED: findings != [])"
    - "All 17 pre-existing G8 + F2 tests pass (regression guard active)"
    - "Three new fixture .md files exist with parser-valid markdown"
    - "Worktree at .worktrees/fix-precision/ is active; main worktree is untouched"
  artifacts:
    - path: ".worktrees/fix-precision"
      provides: "linked worktree for all Phase 8 code changes"
    - path: "tests/fixtures/g8_prose_cited_fp.md"
      provides: "inline prose citation fixture for GATE-01 HIGH branch"
    - path: "tests/fixtures/g8_empty_body_blocker.md"
      provides: "empty External Voices body fixture for GATE-03 BLOCKER branch"
    - path: "tests/fixtures/f2_heading_fp.md"
      provides: "n-gram whose words appear in section headings for GATE-04"
    - path: "tests/unit/test_g8_source_diversity.py"
      provides: "two new methods: test_prose_cited_emits_high, test_empty_body_emits_blocker"
    - path: "tests/unit/test_f2_duplicate_fact.py"
      provides: "two new functions: test_f2_heading_fp_suppressed, test_f2_genuine_phrase_not_suppressed"
  key_links:
    - from: "tests/unit/test_g8_source_diversity.py"
      to: "tests/fixtures/g8_prose_cited_fp.md"
      via: "_load_fixture() in TestG8PartA"
      pattern: "g8_prose_cited_fp"
    - from: "tests/unit/test_f2_duplicate_fact.py"
      to: "tests/fixtures/f2_heading_fp.md"
      via: "_load() in test_f2_heading_fp_suppressed"
      pattern: "f2_heading_fp"
---

<objective>
Create the worktree and write all Wave 0 test scaffolding -- three new fixture
files and four new test functions -- before any source code is touched.

Purpose: Establish the RED state that makes the fix measurable. Every new test
that exercises the changed behavior must fail against the unmodified source,
proving the fixtures and assertions are load-bearing. The 17 existing tests
serve as the regression guard throughout.

Output: Active worktree at .worktrees/fix-precision/, three fixture .md files,
and four new test functions in their respective test files -- in RED state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/houminxi/code/plan-forge/.planning/ROADMAP.md
@/home/houminxi/code/plan-forge/.planning/phases/08-source-code-precision-fixes/08-CONTEXT.md
@/home/houminxi/code/plan-forge/.planning/phases/08-source-code-precision-fixes/08-RESEARCH.md

<interfaces>
From src/plan_forge/checks/epistemic/g8_source_diversity.py (lines 47-52):
  _find_external_voices_body(parsed) -> str | None
  The None branch at check() line 82 returns early, so at line 98 body is
  guaranteed str. No None guard needed inside the citations block.

From src/plan_forge/verdict.py:
  Severity.BLOCKER = "BLOCKER"
  Severity.HIGH    = "HIGH"   (confirmed defined in enum)
  Severity.LOW     = "LOW"

From src/plan_forge/parser.py (citation extraction):
  Only list-item citations ("- Author (Year) ...") match _CITATION_RE.
  Inline prose "Nielsen (2022) showed..." does NOT match -- parsed.citations
  is empty for a fixture using only inline prose.

From tests/unit/test_g8_source_diversity.py (existing patterns to follow):
  _load_fixture(name: str) -> str    -- reads from tests/fixtures/<name>
  TestG8PartA class; check(parsed, []) for Part A mechanical (no LLM clients)

From tests/unit/test_f2_duplicate_fact.py (existing patterns to follow):
  _load(name: str) -> str            -- reads from tests/fixtures/<name>
  f2_duplicate_fact.check(parsed)    -- no extra args for mechanical check

From src/plan_forge/checks/mechanical/f2_duplicate_fact.py:
  _CAP_TOKEN_RE = re.compile(r'\b[A-Z][A-Za-z0-9_-]{2,}\b')
  Matches tokens length >= 3 starting uppercase. "Implementation" and "Tasks"
  both match. _extract_ngrams returns lowercase bigrams/trigrams.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create worktree and verify baseline</name>
  <files>.worktrees/fix-precision (worktree only; no main-tree files changed)</files>

  <read_first>
    - /home/houminxi/code/plan-forge/.gitignore (verify .worktrees/ entry exists)
    - Result of: git -C /home/houminxi/code/plan-forge worktree list
    - Result of: uv run pytest
        tests/unit/test_g8_source_diversity.py
        tests/unit/test_f2_duplicate_fact.py -q
      (baseline: 17 passed expected per RESEARCH.md)
    - /home/houminxi/code/plan-forge/src/plan_forge/verdict.py lines 12-25
      (Severity enum -- confirms HIGH = "HIGH" at line 22; executor need not
      grep; RESEARCH.md open question is RESOLVED)
  </read_first>

  <action>
    From /home/houminxi/code/plan-forge (the main repo root), run:

      git worktree add .worktrees/fix-precision -b fix/precision-v011

    This creates a linked worktree at .worktrees/fix-precision/ on a new branch
    fix/precision-v011. All file edits in Plans 01, 02, and 03 happen inside
    .worktrees/fix-precision/, never in the main worktree.

    Verify .worktrees/ is in .gitignore. If absent, append it and commit with
    message "config: extend gitignore for worktree directory" before proceeding.

    From inside .worktrees/fix-precision/, run:
      uv run pytest tests/unit/test_g8_source_diversity.py
        tests/unit/test_f2_duplicate_fact.py -q

    Record the exact count. This is the regression floor: all existing tests
    must continue to pass after every subsequent task.
  </action>

  <verify>
    <automated>git -C /home/houminxi/code/plan-forge worktree list | grep fix-precision</automated>
  </verify>

  <done>
    Worktree .worktrees/fix-precision/ exists at branch fix/precision-v011.
    Baseline test count confirmed and recorded. Main worktree is clean
    (git status shows no modifications).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: G8 fixtures and failing test stubs (RED)</name>
  <files>
    tests/fixtures/g8_prose_cited_fp.md,
    tests/fixtures/g8_empty_body_blocker.md,
    tests/unit/test_g8_source_diversity.py
  </files>

  <read_first>
    - /home/houminxi/code/plan-forge/tests/fixtures/g8_pass.md
      (parseable citation format: "- Popper (1959). The Logic..." list item)
    - /home/houminxi/code/plan-forge/tests/fixtures/g8_biblio_citation_tp.md
      (existing prose-bullets case; new fixture uses inline prose, not bullets)
    - /home/houminxi/code/plan-forge/tests/unit/test_g8_source_diversity.py
      (full file; add new methods after test_missing_failure_case_blocker)
    - /home/houminxi/code/plan-forge/src/plan_forge/parser.py lines 200-280
      (confirm _CITATION_RE only matches "- Author (Year)" list items)
  </read_first>

  <behavior>
    - test_prose_cited_emits_high: asserts no_cit[0].severity == Severity.HIGH.
      Against unmodified source (always BLOCKER), FAILS. This is the RED gate
      for GATE-01 and GATE-03.
    - test_empty_body_emits_blocker: asserts no_cit[0].severity == Severity.BLOCKER.
      Against unmodified source (always BLOCKER), may PASS. That is acceptable --
      Plan 02 must preserve this pass after the fix.
  </behavior>

  <action>
    All edits inside .worktrees/fix-precision/.

    FIXTURE g8_prose_cited_fp.md: A plan document with an External Voices
    section whose body contains inline prose citation ("Nielsen (2022) showed
    that usability testing catches 85% of issues.") but no list-item citation
    matching "- Author (Year) ...". Because _extract_citations() only captures
    list items, parsed.citations is empty. Include "however" (dissent keyword)
    and "failure" (failure keyword) in the External Voices body so only
    G8.A.no_citation fires, not G8.A.no_dissent or G8.A.no_failure_case.
    Suggested body:

      ## External Voices

      Nielsen (2022) showed that usability testing catches 85% of issues.
      However, critics argue this estimate applies only to consumer software.
      A historical failure case: the 2003 NASA incident taught a lesson about
      skipping usability reviews.

    FIXTURE g8_empty_body_blocker.md: A plan document with an External Voices
    section whose body is empty (only whitespace between the heading line and
    the next heading). The parser will set section.body to "" or a whitespace
    string. body.strip() == "" must be True. Other sections can have any
    content. Example:

      # Plan

      ## Background

      Some background content here.

      ## External Voices

      ## Design

      Design content here.

    TEST STUBS: Inside TestG8PartA class, add two methods after
    test_missing_failure_case_blocker:

      def test_prose_cited_emits_high(self):
          parsed = parse(_load_fixture("g8_prose_cited_fp.md"))
          findings = check(parsed, [])
          no_cit = [f for f in findings
                    if f.check_id == "G8.A.no_citation"]
          assert len(no_cit) == 1
          assert no_cit[0].severity == Severity.HIGH

      def test_empty_body_emits_blocker(self):
          parsed = parse(_load_fixture("g8_empty_body_blocker.md"))
          findings = check(parsed, [])
          no_cit = [f for f in findings
                    if f.check_id == "G8.A.no_citation"]
          assert len(no_cit) == 1
          assert no_cit[0].severity == Severity.BLOCKER

    Step 0 pre-check on the modified test file (run from worktree root):
      python3 -m py_compile tests/unit/test_g8_source_diversity.py
      ruff check tests/unit/test_g8_source_diversity.py
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -P '[^\x00-\x7F]' && echo "FAIL: non-ASCII" || echo "OK"
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]|P[0-9]+:|S[0-9]+:' &&
        echo "FAIL: AI smell" || echo "OK"

    Confirm RED: run test_prose_cited_emits_high in isolation -- it must FAIL.
  </action>

  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/fix-precision && uv run pytest
      tests/unit/test_g8_source_diversity.py::TestG8PartA::test_prose_cited_emits_high
      -xvs 2>&1 | grep -E 'FAILED|AssertionError|BLOCKER|HIGH'
    </automated>
  </verify>

  <done>
    Two G8 fixture files created. test_prose_cited_emits_high FAILS (RED
    confirmed). All 17 pre-existing tests still pass. Step 0 clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: F2 fixture and failing test stubs (RED)</name>
  <files>
    tests/fixtures/f2_heading_fp.md,
    tests/unit/test_f2_duplicate_fact.py
  </files>

  <read_first>
    - /home/houminxi/code/plan-forge/tests/fixtures/f2_fail.md
      (existing TP fixture; sections "Background", "Design", "Implementation",
      "Testing", "Summary" -- "databaseconnection" and "pool" do NOT appear in
      any heading, so the guard will not suppress it)
    - /home/houminxi/code/plan-forge/src/plan_forge/checks/mechanical/f2_duplicate_fact.py
      (full file; see _CAP_TOKEN_RE and _extract_ngrams to understand capture
      rules; _FINDING_CAP = 20)
    - /home/houminxi/code/plan-forge/tests/unit/test_f2_duplicate_fact.py
      (full file; add new functions after test_f2_edge_exactly_two_sections)
  </read_first>

  <behavior>
    - test_f2_heading_fp_suppressed: asserts findings == []. Against unmodified
      source (no heading guard), at least one F2.duplicate_fact finding fires
      for "implementation tasks" -- test FAILS. RED gate for GATE-04.
    - test_f2_genuine_phrase_not_suppressed: asserts "F2.duplicate_fact" in ids
      using f2_fail.md. Against unmodified source, this PASSES.
      Plan 03 must keep it passing after the fix.
  </behavior>

  <action>
    All edits inside .worktrees/fix-precision/.

    FIXTURE f2_heading_fp.md: Use the exact design from RESEARCH.md. The
    section heading "Implementation Tasks" contributes both "implementation"
    and "tasks" to heading_words. The n-gram "implementation tasks" appears
    in 4+ section bodies (>= 3 distinct headings), so F2 would fire without
    the guard. Content:

      # Plan

      ## Background

      Implementation Tasks must be prioritized carefully.
      Implementation Tasks define the project scope.

      ## Implementation Tasks

      Implementation Tasks are listed below.

      ## Design

      Implementation Tasks emerge from design.

      ## Delivery

      Final Implementation Tasks get signed off here.

    Before adding the test stub, verify RED state by running the fixture
    against unmodified f2_duplicate_fact.py:
      uv run pytest -k "test_f2_heading_fp_suppressed" -xvs
    If f2_heading_fp.md produces zero findings against unmodified source
    (fixture is invalid), revise the fixture so "implementation tasks" appears
    in >= 3 distinct section bodies.

    TEST STUBS: Add two functions after test_f2_edge_exactly_two_sections:

      def test_f2_heading_fp_suppressed():
          parsed = parse(_load("f2_heading_fp.md"))
          findings = f2_duplicate_fact.check(parsed)
          assert findings == [], (
              f"Expected heading-derived n-gram suppressed, got: {findings}"
          )

      def test_f2_genuine_phrase_not_suppressed():
          parsed = parse(_load("f2_fail.md"))
          findings = f2_duplicate_fact.check(parsed)
          ids = [f.check_id for f in findings]
          assert "F2.duplicate_fact" in ids, (
              f"Expected genuine repetition still fires, got: {ids}"
          )

    Step 0 pre-check on modified test file:
      python3 -m py_compile tests/unit/test_f2_duplicate_fact.py
      ruff check tests/unit/test_f2_duplicate_fact.py
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -P '[^\x00-\x7F]' && echo "FAIL: non-ASCII" || echo "OK"
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]|P[0-9]+:|S[0-9]+:' &&
        echo "FAIL: AI smell" || echo "OK"
  </action>

  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/fix-precision && uv run pytest
      tests/unit/test_f2_duplicate_fact.py -q 2>&1 | tail -10
    </automated>
  </verify>

  <done>
    f2_heading_fp.md exists. test_f2_heading_fp_suppressed FAILS against
    unmodified source (RED confirmed). test_f2_genuine_phrase_not_suppressed
    passes. All pre-existing F2 tests pass. Step 0 clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture markdown -> parser | Fixed-content .md files; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | no new packages installed | accept | no package manager installs in this plan; legitimacy gate not applicable |
</threat_model>

<verification>
From inside .worktrees/fix-precision/:

  uv run pytest tests/unit/test_g8_source_diversity.py
    tests/unit/test_f2_duplicate_fact.py -q

Expected: 19 passed, 2 failed -- test_prose_cited_emits_high and
test_f2_heading_fp_suppressed in FAILED. All 17 pre-existing tests pass.
(test_empty_body_emits_blocker and test_f2_genuine_phrase_not_suppressed
may pass against unmodified source -- that is acceptable.)
</verification>

<success_criteria>
1. git worktree list shows .worktrees/fix-precision at fix/precision-v011
2. tests/fixtures/g8_prose_cited_fp.md, g8_empty_body_blocker.md, f2_heading_fp.md exist
3. test_prose_cited_emits_high: FAILED (BLOCKER != HIGH)
4. test_f2_heading_fp_suppressed: FAILED (findings != [])
5. All 17 pre-existing G8 + F2 unit tests: PASSED
6. Step 0 clean on both modified test files
7. Main worktree git status: clean (no modifications)
</success_criteria>

<output>
Create .planning/phases/08-source-code-precision-fixes/08-01-SUMMARY.md when done.
</output>
