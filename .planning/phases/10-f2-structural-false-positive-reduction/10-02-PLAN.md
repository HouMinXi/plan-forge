---
phase: 10-f2-structural-false-positive-reduction
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/plan_forge/checks/mechanical/f2_duplicate_fact.py
autonomous: false
requirements:
  - F2-01
  - F2-02

must_haves:
  truths:
    - "test_f2_structural_fp_suppressed PASSES (GREEN: structural phrases silenced)"
    - "test_f2_signoff_fp_suppressed PASSES (GREEN: signoff author names silenced)"
    - "test_f2_heading_fp_suppressed PASSES (Phase 8 heading guard preserved)"
    - "test_f2_fail_fixture PASSES (genuine repetition still fires)"
    - "All pre-existing F2 tests still pass (no regression)"
    - "Bug-inject cycle completed: remove union guard -> structural-term FP fires -> restore guard -> silence; TP unaffected"
    - "Full test suite green (764 baseline + new tests, minus 1 pre-existing version failure)"
  artifacts:
    - path: "src/plan_forge/checks/mechanical/f2_duplicate_fact.py"
      provides: "F2 check with _STRUCTURAL_WORDS frozenset, _SIGNOFF_RE regex, signoff_words extraction, and union exclusion filter"
      contains: "_STRUCTURAL_WORDS"
  key_links:
    - from: "f2_duplicate_fact.py flagged loop"
      to: "exclusion set"
      via: "set(ng.split()) <= exclusion subset test"
      pattern: "exclusion"
    - from: "f2_duplicate_fact.py signoff_words build"
      to: "parsed.raw_text"
      via: "_SIGNOFF_RE.match on each line"
      pattern: "_SIGNOFF_RE"
---

<objective>
Apply the F2 structural-word and signoff-word suppression: add _STRUCTURAL_WORDS
frozenset and _SIGNOFF_RE regex as module-level constants, build signoff_words
from parsed.raw_text, replace the heading_words-only guard with a union
exclusion filter. Run the bug-inject cycle (GATE-10T) before committing.

Purpose: Implementation plans repeat structural terms like "Step N: Run ...",
"Expected: Pass", and "Signed-off-by: Name" across every task section by
design. F2 treated these as duplicate-fact violations, producing up to 20 LOW
findings on a real-world 28-task plan. The union exclusion filter targets
exactly these structural terms and dynamic author names without touching the
genuine TP case ("DatabaseConnection Pool") whose words do not appear in any
exclusion set.

Output: Modified f2_duplicate_fact.py with GREEN tests for F2-01 and F2-02;
bug-inject teeth verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/houminxi/code/plan-forge/.planning/phases/10-f2-structural-false-positive-reduction/10-CONTEXT.md
@/home/houminxi/code/plan-forge/.planning/phases/10-f2-structural-false-positive-reduction/10-RESEARCH.md
@/home/houminxi/code/plan-forge/.planning/phases/10-f2-structural-false-positive-reduction/10-01-SUMMARY.md

<interfaces>
From src/plan_forge/checks/mechanical/f2_duplicate_fact.py (current, post-Phase 8, 83 lines):

  Line 9:   import re
  Line 15:  _CAP_TOKEN_RE = re.compile(r'\b[A-Z][A-Za-z0-9_-]{2,}\b')
  Line 18:  _FINDING_CAP = 20

  Lines 61-68 (heading_words guard -- will be extended):
      heading_words: set[str] = set()
      for heading in parsed.sections:
          heading_words.update(heading.lower().split())

      findings: list[Finding] = []
      for ng, headings in flagged[:_FINDING_CAP]:
          if set(ng.split()) <= heading_words:
              continue

  Line 67: the guard line `if set(ng.split()) <= heading_words:` is REPLACED by
           `if set(ng.split()) <= exclusion:` -- not a second separate guard.

From src/plan_forge/parser.py line 70:
  ParsedPlan.raw_text: str  # full plan text as a single string

Key constraint (RESEARCH.md Critical Finding):
  The union MUST include heading_words. Using only _STRUCTURAL_WORDS | signoff_words
  regresses test_f2_heading_fp_suppressed (Phase 8). The correct formula is:
    exclusion = _STRUCTURAL_WORDS | signoff_words | heading_words
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply union exclusion filter and verify GREEN</name>
  <files>src/plan_forge/checks/mechanical/f2_duplicate_fact.py</files>

  <read_first>
    - /home/houminxi/code/plan-forge/src/plan_forge/checks/mechanical/f2_duplicate_fact.py
      (full file, 83 lines; edit target: add constants after line 18, insert
      signoff_words build after line 63, replace guard at line 67)
    - /home/houminxi/code/plan-forge/tests/fixtures/f2_fail.md
      (existing TP fixture; confirm its headings do NOT contain "databaseconnection"
      or "pool", ensuring the guard will not suppress genuine findings)
    - /home/houminxi/code/plan-forge/.planning/phases/10-f2-structural-false-positive-reduction/10-01-SUMMARY.md
      (confirm Plan 01 baseline: exact test count and RED state)
  </read_first>

  <behavior>
    After the edit:
    - test_f2_structural_fp_suppressed: PASSES (all words of "run expected",
      "expected pass", "run expected pass" are in _STRUCTURAL_WORDS; subset
      test is True; findings suppressed)
    - test_f2_signoff_fp_suppressed: PASSES (signoff_words contains "minxi"
      and "hou" extracted from Signed-off-by lines in raw_text; the bigram
      "minxi hou" has all words in exclusion; suppressed)
    - test_f2_heading_fp_suppressed: PASSES (heading_words still included in
      exclusion; "implementation tasks" subset test is True; no regression)
    - test_f2_fail_fixture: PASSES (genuine phrase "databaseconnection pool"
      has words not in any exclusion set; fires as before)
    - test_f2_pass_fixture: PASSES (no flagged ngrams)
    - test_f2_severity: PASSES (LOW severity unchanged)
    - test_f2_edge_exactly_two_sections: PASSES (below threshold)
    - test_f2_genuine_phrase_not_suppressed: PASSES (fires as before)
  </behavior>

  <acceptance_criteria>
    - f2_duplicate_fact.py contains _STRUCTURAL_WORDS frozenset with exactly
      13 tokens: step, run, expected, pass, files, create, modify, append,
      commit, test, write, add, signed-off-by (per D-03)
    - f2_duplicate_fact.py contains _SIGNOFF_RE compiled regex (per D-04)
    - f2_duplicate_fact.py contains signoff_words extraction loop scanning
      parsed.raw_text.splitlines() (per D-04)
    - Line 67 guard reads `if set(ng.split()) <= exclusion:` not
      `if set(ng.split()) <= heading_words:` (per D-01 corrected)
    - exclusion is built as `_STRUCTURAL_WORDS | signoff_words | heading_words`
    - All 8 F2 tests pass (6 existing + 2 new from Plan 10-01)
    - Step 0 clean on f2_duplicate_fact.py diff
  </acceptance_criteria>

  <action>
    Edit inside .worktrees/f2-structural-fp/ only.

    PART A -- Add module-level constants after _FINDING_CAP = 20 (line 18),
    before the def _extract_ngrams() function:

      _STRUCTURAL_WORDS: frozenset[str] = frozenset({
          "step", "run", "expected", "pass", "files",
          "create", "modify", "append", "commit", "test",
          "write", "add", "signed-off-by",
      })

      _SIGNOFF_RE = re.compile(r"^\s*signed-off-by\s*:\s*(.*)", re.IGNORECASE)

    The `re` module is already imported at line 9. No new imports needed (per D-02).

    PART B -- Inside check(), after the heading_words build (after line 63,
    end of `heading_words.update(heading.lower().split())`), insert the
    signoff_words extraction and exclusion set build:

      signoff_words: set[str] = set()
      for line in parsed.raw_text.splitlines():
          m = _SIGNOFF_RE.match(line)
          if m:
              name = re.sub(r"<[^>]+>", "", m.group(1)).strip()
              signoff_words.update(t.lower() for t in name.split())
      exclusion = _STRUCTURAL_WORDS | signoff_words | heading_words

    Use parsed.raw_text (not section.body) per D-04 to catch Signed-off-by
    lines inside XML/code blocks the section parser may exclude. Use
    re.sub(r"<[^>]+>", "", ...) to strip email addresses before tokenizing.

    PART C -- Replace the existing guard at line 67. Change:

      if set(ng.split()) <= heading_words:

    to:

      if set(ng.split()) <= exclusion:

    This is the single union filter per D-01. Do NOT add a second separate
    guard -- cross-category n-grams like "signed-off-by minxi" need one word
    from _STRUCTURAL_WORDS and one from signoff_words, and two separate guards
    each requiring ALL words in their own set would miss such cross-category
    phrases.

    The three-way union (_STRUCTURAL_WORDS | signoff_words | heading_words)
    preserves Phase 8 heading suppression. Omitting heading_words would cause
    test_f2_heading_fp_suppressed to regress.

    Step 0 pre-check on modified source file:
      python3 -m py_compile
        src/plan_forge/checks/mechanical/f2_duplicate_fact.py
      ruff check src/plan_forge/checks/mechanical/f2_duplicate_fact.py
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -P '[^\x00-\x7F]' && echo "FAIL: non-ASCII" || echo "OK"
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]|P[0-9]+:|S[0-9]+:' &&
        echo "FAIL: AI smell" || echo "OK"

    Run F2 tests to confirm GREEN:
      uv run pytest tests/unit/test_f2_duplicate_fact.py -q
    Expected: 8 passed, 0 failed.
  </action>

  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/f2-structural-fp && uv run pytest
      tests/unit/test_f2_duplicate_fact.py -q 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    All 8 F2 tests pass including test_f2_structural_fp_suppressed (suppressed),
    test_f2_signoff_fp_suppressed (suppressed), test_f2_heading_fp_suppressed
    (Phase 8 preserved), and test_f2_fail_fixture (genuine TP still fires).
    Step 0 clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bug-inject cycle GATE-10T (manual teeth verification)</name>

  <what-built>
    F2 source fix is GREEN. Before committing, the implementer must perform
    the bug-inject cycle to confirm the union exclusion guard is the operative
    suppression -- not that F2 is inadvertently disabled for all inputs.
  </what-built>

  <how-to-verify>
    Perform the following steps from inside .worktrees/f2-structural-fp/:

    STEP A -- Remove the union guard (working-tree only, no commit):
    In f2_duplicate_fact.py, temporarily comment out the signoff_words build,
    the exclusion set build, and revert the guard to heading_words only:

      # signoff_words: set[str] = set()
      # for line in parsed.raw_text.splitlines():
      #     m = _SIGNOFF_RE.match(line)
      #     if m:
      #         name = re.sub(r"<[^>]+>", "", m.group(1)).strip()
      #         signoff_words.update(t.lower() for t in name.split())
      # exclusion = _STRUCTURAL_WORDS | signoff_words | heading_words

    And change the guard back from:
      if set(ng.split()) <= exclusion:
    to:
      if set(ng.split()) <= heading_words:

    STEP B -- Run test_f2_structural_fp_suppressed with injected bug:
      uv run pytest
        tests/unit/test_f2_duplicate_fact.py::test_f2_structural_fp_suppressed
        -xvs

    Expected: FAILED (AssertionError: findings != [])
    This confirms the structural-term FP fixture fires F2 findings when the
    new guard is removed.

    STEP C -- Restore the fix:
      git checkout -- src/plan_forge/checks/mechanical/f2_duplicate_fact.py

    STEP D -- Confirm the fix is back and tests pass:
      uv run pytest
        tests/unit/test_f2_duplicate_fact.py::test_f2_structural_fp_suppressed
        -xvs
    Expected: PASSED (silence)

      uv run pytest
        tests/unit/test_f2_duplicate_fact.py::test_f2_signoff_fp_suppressed
        -xvs
    Expected: PASSED (silence)

    STEP E -- Confirm the TP still fires after restore:
      uv run pytest
        tests/unit/test_f2_duplicate_fact.py::test_f2_fail_fixture
        -xvs
    Expected: PASSED (genuine phrase still fires F2 findings)

    Record the result for the commit message:
      Fixture: f2_structural_fp.md
      Before restore (guard removed): 1+ F2 findings for structural phrases
      After restore: 0 findings (suppressed)
      TP (f2_fail.md): still fires after restore (unaffected)
  </how-to-verify>

  <resume-signal>
    Type "gate10t-confirmed: structural FP fires when guard removed, silenced
    when restored, TP unaffected" to confirm bug-inject cycle passed, then
    proceed to commit.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Full suite green and commit F2 fix</name>
  <files>src/plan_forge/checks/mechanical/f2_duplicate_fact.py</files>

  <read_first>
    - Result of Task 2 (bug-inject cycle outcome -- finding count before/after)
    - git -C /home/houminxi/code/plan-forge log --oneline -3 (commit style ref)
  </read_first>

  <acceptance_criteria>
    - Full test suite passes (764+ baseline + 2 new F2 tests, minus 1
      pre-existing test_version_is_alpha2 failure which is unrelated)
    - Commit on fix/f2-structural-fp-v012 with human-voice message
    - Commit message records bug-inject result from GATE-10T
    - No AI smell in commit (no task IDs, no model names, no Co-Authored-By)
    - Signed-off-by: Minxi Hou <houminxi@gmail.com>
  </acceptance_criteria>

  <action>
    From inside .worktrees/f2-structural-fp/, run full suite:
      uv run pytest tests/ -q

    Must pass 764+ baseline tests plus the 2 new F2 tests (total 766+).
    1 pre-existing failure (test_version_is_alpha2) is expected and unrelated.
    If any other pre-existing test fails that was passing before this plan,
    stop and diagnose.

    If green, stage and commit with a human-voice message. The message body
    records the bug-inject result from GATE-10T. No task IDs, no model names,
    no review metadata, no Co-Authored-By AI lines. Use this structure:

      checks/f2: suppress structural template phrases and signoff tokens

      Implementation plans repeat structural terms like "Step N: Run ...",
      "Expected: Pass", and "Signed-off-by: Name" across every task section
      by design. F2 treated these as duplicate-fact violations, producing up
      to 20 LOW findings on a real-world 28-task plan.

      Add a 13-token frozenset of structural vocabulary and a dynamic set of
      Signed-off-by author tokens (extracted from raw plan text). Union both
      with the existing heading_words set; skip any flagged n-gram whose words
      are all in the combined exclusion set.

      Bug-inject confirmed: removing the guard causes f2_structural_fp.md to
      fire F2 findings. Restoring silences it. TP fixture f2_fail.md continues
      to fire (genuine phrases unaffected). Real-world check: 0 F2 LOW after
      fix on antishirk-m1-plan.md (was 20).

      Signed-off-by: Minxi Hou <houminxi@gmail.com>

    Stage the source file and the F2-related test artifacts created in Plan 01:
      git add src/plan_forge/checks/mechanical/f2_duplicate_fact.py
        tests/fixtures/f2_structural_fp.md
        tests/fixtures/f2_signoff_fp.md
        tests/unit/test_f2_duplicate_fact.py

    Note: this commit is on the feature branch fix/f2-structural-fp-v012. It
    is a pre-review commit. Plan 10-03 runs the full forge review pipeline;
    if the review sub-session produces findings, amendment commits may be added
    to this branch before merge.
  </action>

  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/f2-structural-fp &&
      uv run pytest tests/ -q 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    Full suite green. F2 fix committed on fix/f2-structural-fp-v012. Commit
    message is human-voice with bug-inject result recorded. No AI smell in
    commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed.raw_text -> signoff_words set | Internal Python; raw_text from parser, no user input at runtime |
| parsed.sections dict keys -> heading_words set | Internal Python; section keys from parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | f2_duplicate_fact.py union exclusion logic | mitigate | bug-inject cycle (GATE-10T) confirms guard is load-bearing; TP test confirms over-suppression is not occurring |
| T-10-02 | Information Disclosure | _SIGNOFF_RE extracts author names | accept | names are used only as lowercase tokens in memory for subset test; never logged or persisted; no PII exposure beyond what is already in the plan text |
| T-10-SC | Tampering | no new packages installed | accept | no package manager installs; legitimacy gate not applicable |
</threat_model>

<verification>
From inside .worktrees/f2-structural-fp/:

  uv run pytest tests/unit/test_f2_duplicate_fact.py -v

All tests pass including:
  test_f2_structural_fp_suppressed -- PASSED (structural phrases suppressed)
  test_f2_signoff_fp_suppressed -- PASSED (signoff names suppressed)
  test_f2_heading_fp_suppressed -- PASSED (Phase 8 heading guard preserved)
  test_f2_genuine_phrase_not_suppressed -- PASSED (genuine phrase still fires)
  test_f2_fail_fixture -- PASSED
  test_f2_pass_fixture -- PASSED
  test_f2_severity -- PASSED
  test_f2_edge_exactly_two_sections -- PASSED
</verification>

<success_criteria>
1. f2_duplicate_fact.py contains _STRUCTURAL_WORDS frozenset (13 tokens per D-03)
2. f2_duplicate_fact.py contains _SIGNOFF_RE regex and signoff_words extraction
3. Guard line reads `if set(ng.split()) <= exclusion:` with
   `exclusion = _STRUCTURAL_WORDS | signoff_words | heading_words`
4. All 8 F2 tests pass (6 existing + 2 new)
5. Full suite: 764+ pass, 0 new failures (1 pre-existing version test excluded)
6. Bug-inject cycle GATE-10T confirmed (structural FP fires when guard
   removed; TP unaffected after restore)
7. Commit on fix/f2-structural-fp-v012 with human-voice message and Signed-off-by
8. Step 0 clean on f2_duplicate_fact.py diff
</success_criteria>

<output>
Create .planning/phases/10-f2-structural-false-positive-reduction/10-02-SUMMARY.md when done.
</output>
