---
phase: 10-f2-structural-false-positive-reduction
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - .planning/phases/10-f2-structural-false-positive-reduction/10-REVIEW-RESULT.md
autonomous: false

requirements:
  - F2-01
  - F2-02

must_haves:
  truths:
    - "Forge review pipeline completed (3 cycles, 9 passes minimum, zero findings)"
    - "Step 4 smoke test passed (full suite green after review confirms no drift)"
    - "Branch fix/f2-structural-fp-v012 merged into main"
    - "Worktree .worktrees/f2-structural-fp removed after merge"
    - "After merge to main, full suite passes with test_f2_structural_fp_suppressed and test_f2_signoff_fp_suppressed in PASSED state"
  artifacts:
    - path: "fix/f2-structural-fp-v012 (branch)"
      provides: "all Phase 10 commits ready for merge to main"
  key_links:
    - from: "forge review sub-session"
      to: "f2_duplicate_fact.py diff"
      via: "human forwards /tmp/draft_forge_review_phase10.txt prompt to separate Claude session"
      pattern: "review sub-session separate from implementation"
---

<objective>
Run the full forge review pipeline on the Phase 10 diff, then merge to main
and clean up the worktree.

Purpose: CLAUDE.md requires implementation and review agents to be separate
sub-sessions. This plan is a checkpoint that pauses for the user to forward
the review prompt to a separate Claude session, wait for the zero-findings
result, then merge.

Output: Phase 10 changes merged to main; fix/f2-structural-fp-v012 branch
retired; worktree removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/houminxi/code/plan-forge/.planning/phases/10-f2-structural-false-positive-reduction/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare review prompt and run Step 0</name>
  <files>/tmp/draft_forge_review_phase10.txt (review prompt file)</files>

  <read_first>
    - git -C /home/houminxi/code/plan-forge diff main...fix/f2-structural-fp-v012
      (full diff of all Phase 10 changes -- F2 fix + fixtures + tests)
    - /home/houminxi/code/plan-forge/CLAUDE.md section "Code Quality Gates"
      (review pipeline requirements: Step 0 then 3-cycle static review)
  </read_first>

  <acceptance_criteria>
    - Step 0a (syntax), 0b (lint), 0c (non-ASCII), 0d (AI smell) all clean
    - /tmp/draft_forge_review_phase10.txt exists with full diff and review
      instructions
  </acceptance_criteria>

  <action>
    From inside .worktrees/f2-structural-fp/, run Step 0 on the combined diff:

    0a Syntax:
      python3 -m py_compile
        src/plan_forge/checks/mechanical/f2_duplicate_fact.py

    0b Lint:
      ruff check src/plan_forge/checks/mechanical/f2_duplicate_fact.py

    0c Non-ASCII:
      git diff main...fix/f2-structural-fp-v012 --diff-filter=AM -U0 |
        grep '^+' | grep -P '[^\x00-\x7F]' &&
        echo "FAIL: non-ASCII" || echo "OK"

    0d AI-smell:
      git diff main...fix/f2-structural-fp-v012 --diff-filter=AM -U0 |
        grep '^+' |
        grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]|P[0-9]+:|S[0-9]+:' &&
        echo "FAIL: AI smell" || echo "OK"

    If any Step 0 check fails, fix before proceeding to forge review.

    Then prepare the forge review prompt. Write to
    /tmp/draft_forge_review_phase10.txt the full diff and the instruction:

      Review the following diff for plan-forge v0.1.2 Phase 10 changes.
      Apply the full forge review pipeline (qodo-review, code-review-expert,
      adversarial-qe -- 3 cycles, 9 passes minimum). Report all findings.
      Exit criteria: all 3 passes in the final cycle produce zero findings.

      [paste full git diff output here]

    Output the path /tmp/draft_forge_review_phase10.txt for the user to forward
    to a separate review sub-session.
  </action>

  <verify>
    <automated>
      git -C /home/houminxi/code/plan-forge diff main...fix/f2-structural-fp-v012
      --stat 2>&1 | tail -5
    </automated>
  </verify>

  <done>
    Step 0 all clean. Review prompt written to /tmp/draft_forge_review_phase10.txt.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Forward to review sub-session and await zero findings</name>

  <what-built>
    Review prompt is at /tmp/draft_forge_review_phase10.txt with the full
    Phase 10 diff. Step 0 is clean.
  </what-built>

  <how-to-verify>
    1. Open a new Claude session (separate from this implementation session).
    2. Paste the contents of /tmp/draft_forge_review_phase10.txt into the new
       session.
    3. Have the review session run qodo-review, code-review-expert, and
       adversarial-qe -- 3 full cycles, 9 passes minimum.
    4. For each finding: fix in the implementation worktree, re-run Step 0,
       re-run tests, then bring the fix back to the review session for
       re-review. Do not shortcut cycles.
    5. Exit criteria: all 3 passes in the final cycle report zero findings.
    6. After zero findings: run Step 4 smoke test (full suite):
         uv run pytest tests/ -q
       Must pass.
    7. Instruct the review sub-session to write its final result to:
         .planning/phases/10-f2-structural-false-positive-reduction/10-REVIEW-RESULT.md
       The file must contain either "PASS: 3 cycles zero findings" or list
       all remaining findings if not yet clean. The implementation session
       reads this file in Task 3 before merging.
  </how-to-verify>

  <resume-signal>
    Type "forge-review-clean: 3 cycles zero findings, Step 4 smoke test passed"
    to resume and proceed to merge.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Merge branch and remove worktree</name>
  <files>main branch (merge target), .worktrees/f2-structural-fp (removed), .planning/phases/10-f2-structural-false-positive-reduction/10-REVIEW-RESULT.md (read-only gate)</files>

  <read_first>
    - .planning/phases/10-f2-structural-false-positive-reduction/10-REVIEW-RESULT.md
      (must exist and contain "PASS" or "zero findings" before proceeding;
      if file is absent or shows remaining findings, do not merge -- return
      to Task 2 review loop)
    - git -C /home/houminxi/code/plan-forge log fix/f2-structural-fp-v012 --oneline
      (confirm all Phase 10 commits are on branch)
    - git -C /home/houminxi/code/plan-forge log main --oneline -3 (merge base)
  </read_first>

  <acceptance_criteria>
    - 10-REVIEW-RESULT.md contains "PASS" or "zero findings"
    - Merge commit on main includes the F2 structural fix
    - Full suite passes on main after merge
    - Worktree .worktrees/f2-structural-fp removed
    - Branch fix/f2-structural-fp-v012 deleted
    - STATE.md updated: phase 10 complete
  </acceptance_criteria>

  <action>
    From the main repo root /home/houminxi/code/plan-forge (not inside
    the worktree):

    Merge fix/f2-structural-fp-v012 into main:
      git checkout main
      git merge --no-ff fix/f2-structural-fp-v012 -m "checks/f2: structural + signoff FP suppression (v0.1.2)"

    The merge commit message summarizes the fix without AI smell.

    Verify the merged state:
      uv run pytest tests/ -q

    Full suite must pass on main after merge.

    Remove the worktree and delete the branch:
      git worktree remove .worktrees/f2-structural-fp
      git branch -d fix/f2-structural-fp-v012

    Update STATE.md: set current phase to 10, status to "Phase 10 complete",
    milestone v0.1.2 complete.

    Update ROADMAP.md: mark Phase 10 plans complete, update progress table.
  </action>

  <verify>
    <automated>
      grep -iE 'PASS|zero findings' /home/houminxi/code/plan-forge/.planning/phases/10-f2-structural-false-positive-reduction/10-REVIEW-RESULT.md &&
      git -C /home/houminxi/code/plan-forge worktree list &&
      git -C /home/houminxi/code/plan-forge log --oneline -5
    </automated>
  </verify>

  <done>
    10-REVIEW-RESULT.md confirmed "PASS" before merge. fix/f2-structural-fp-v012
    merged to main. Worktree removed. Branch deleted. Full suite passes on
    main (with test_f2_structural_fp_suppressed and test_f2_signoff_fp_suppressed
    PASSED, test_f2_heading_fp_suppressed PASSED, test_f2_fail_fixture PASSED).
    STATE.md and ROADMAP.md updated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review sub-session -> findings | Separate Claude session; findings are advisory until human confirms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Repudiation | forge review bypassed | mitigate | checkpoint:human-action gate; resume-signal documents review outcome; impl != reviewer enforced by separate session requirement |
| T-10-SC | Tampering | no new packages installed | accept | no package manager installs in this plan |
</threat_model>

<verification>
After merge:

  git -C /home/houminxi/code/plan-forge log --oneline -5
  (should show merge commit + F2 fix commit)

  uv run pytest /home/houminxi/code/plan-forge/tests/ -q
  (764+ passed, 0 new failures, suite includes all Phase 10 new tests)

  git -C /home/houminxi/code/plan-forge worktree list
  (only main worktree listed; f2-structural-fp removed)
</verification>

<success_criteria>
1. Forge review: 3 cycles, 9 passes, zero findings in final cycle
2. Step 4 smoke test: uv run pytest tests/ -q passes on fix/f2-structural-fp-v012
   after review cycle
3. Merge commit on main includes the F2 structural/signoff suppression fix
4. Full suite passes on main after merge (766+ passing)
5. Worktree .worktrees/f2-structural-fp removed
6. Branch fix/f2-structural-fp-v012 deleted
7. STATE.md updated: phase 10 complete, v0.1.2 shipped
</success_criteria>

<output>
Create .planning/phases/10-f2-structural-false-positive-reduction/10-03-SUMMARY.md when done.
</output>
