---
phase: 10-f2-structural-false-positive-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .worktrees/f2-structural-fp (worktree created)
  - tests/fixtures/f2_structural_fp.md
  - tests/fixtures/f2_signoff_fp.md
  - tests/unit/test_f2_duplicate_fact.py
autonomous: true
requirements:
  - F2-01
  - F2-02

must_haves:
  truths:
    - "test_f2_structural_fp_suppressed FAILS against unmodified source (RED: findings != [])"
    - "test_f2_signoff_fp_suppressed FAILS against unmodified source (RED: findings != [])"
    - "All 6 pre-existing F2 tests still pass (regression guard active)"
    - "Two new fixture .md files exist with parser-valid markdown"
    - "Worktree at .worktrees/f2-structural-fp/ is active; main worktree is untouched"
    - "All 6 pre-existing F2 tests PASS including test_f2_fail_fixture (genuine TP not suppressed)"
  artifacts:
    - path: ".worktrees/f2-structural-fp"
      provides: "linked worktree for all Phase 10 code changes"
    - path: "tests/fixtures/f2_structural_fp.md"
      provides: "structural template phrases repeated across 4 sections for F2-01"
    - path: "tests/fixtures/f2_signoff_fp.md"
      provides: "Signed-off-by author names repeated across 3 sections for F2-02"
    - path: "tests/unit/test_f2_duplicate_fact.py"
      provides: "two new functions: test_f2_structural_fp_suppressed, test_f2_signoff_fp_suppressed"
  key_links:
    - from: "tests/unit/test_f2_duplicate_fact.py"
      to: "tests/fixtures/f2_structural_fp.md"
      via: "_load() in test_f2_structural_fp_suppressed"
      pattern: "f2_structural_fp"
    - from: "tests/unit/test_f2_duplicate_fact.py"
      to: "tests/fixtures/f2_signoff_fp.md"
      via: "_load() in test_f2_signoff_fp_suppressed"
      pattern: "f2_signoff_fp"
---

<objective>
Create the worktree and write all Wave 0 test scaffolding -- two new fixture
files and two new test functions -- before any source code is touched.

Purpose: Establish the RED state that makes the fix measurable. Each new test
must fail against the unmodified source, proving the fixtures and assertions
are load-bearing. The 6 existing F2 tests serve as the regression guard.

Output: Active worktree at .worktrees/f2-structural-fp/, two fixture .md files,
and two new test functions in RED state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/houminxi/code/plan-forge/.planning/ROADMAP.md
@/home/houminxi/code/plan-forge/.planning/phases/10-f2-structural-false-positive-reduction/10-CONTEXT.md
@/home/houminxi/code/plan-forge/.planning/phases/10-f2-structural-false-positive-reduction/10-RESEARCH.md

<interfaces>
From src/plan_forge/checks/mechanical/f2_duplicate_fact.py (current, post-Phase 8):

  _CAP_TOKEN_RE = re.compile(r'\b[A-Z][A-Za-z0-9_-]{2,}\b')
  _FINDING_CAP = 20

  def _extract_ngrams(text: str) -> set[str]:
      tokens = _CAP_TOKEN_RE.findall(text)
      lower = [t.lower() for t in tokens]
      # returns lowercase bigrams and trigrams of capitalized tokens

  def check(parsed: ParsedPlan, **kwargs) -> list[Finding]:
      # lines 61-68: heading_words guard (Phase 8)
      heading_words: set[str] = set()
      for heading in parsed.sections:
          heading_words.update(heading.lower().split())
      findings: list[Finding] = []
      for ng, headings in flagged[:_FINDING_CAP]:
          if set(ng.split()) <= heading_words:
              continue
          findings.append(Finding(...))

Key facts for fixture design:
  - _CAP_TOKEN_RE captures tokens starting uppercase, length >= 3 characters.
  - "Run" (3 chars), "Step" (4 chars), "Expected" (8 chars), "Pass" (4 chars)
    all match. "Minxi" (5 chars), "Hou" (3 chars) also match.
  - _extract_ngrams produces lowercase bigrams/trigrams from matched tokens.
  - Fixture must have ngrams in >= 3 distinct section bodies to trigger F2.

From tests/unit/test_f2_duplicate_fact.py (existing pattern):
  _FIXTURES = pathlib.Path(__file__).parent.parent / "fixtures"
  def _load(name: str) -> str:
      return (_FIXTURES / name).read_text(encoding="utf-8")

From src/plan_forge/parser.py line 70:
  ParsedPlan.raw_text: str  # full plan text (used for signoff extraction)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create worktree and verify baseline</name>
  <files>.worktrees/f2-structural-fp (worktree only; no main-tree files changed)</files>

  <read_first>
    - /home/houminxi/code/plan-forge/.gitignore (verify .worktrees/ entry exists)
    - Result of: git -C /home/houminxi/code/plan-forge worktree list
    - Result of: cd /home/houminxi/code/plan-forge && uv run pytest
        tests/unit/test_f2_duplicate_fact.py -q
      (baseline: 6 passed expected per RESEARCH.md "Current F2 tests: 6 passing")
  </read_first>

  <acceptance_criteria>
    - git worktree list shows .worktrees/f2-structural-fp at fix/f2-structural-fp-v012
    - Baseline F2 test count is 6 passed
    - Main worktree git status is clean
  </acceptance_criteria>

  <action>
    From /home/houminxi/code/plan-forge (the main repo root), run:

      git worktree add .worktrees/f2-structural-fp -b fix/f2-structural-fp-v012

    This creates a linked worktree on a new branch fix/f2-structural-fp-v012.
    All file edits in Plans 10-01, 10-02, and 10-03 happen inside
    .worktrees/f2-structural-fp/, never in the main worktree.

    Verify .worktrees/ is in .gitignore. If absent, append it.

    From inside .worktrees/f2-structural-fp/, run:
      uv run pytest tests/unit/test_f2_duplicate_fact.py -q

    Record the exact count. Expected: 6 passed. This is the regression floor.
  </action>

  <verify>
    <automated>git -C /home/houminxi/code/plan-forge worktree list | grep f2-structural-fp</automated>
  </verify>

  <done>
    Worktree .worktrees/f2-structural-fp/ exists at branch fix/f2-structural-fp-v012.
    Baseline: 6 F2 tests passed. Main worktree is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixture files and failing test stubs (RED)</name>
  <files>
    tests/fixtures/f2_structural_fp.md,
    tests/fixtures/f2_signoff_fp.md,
    tests/unit/test_f2_duplicate_fact.py
  </files>

  <read_first>
    - /home/houminxi/code/plan-forge/tests/fixtures/f2_fail.md
      (existing TP fixture; sections "Background", "Design", "Implementation",
      "Testing", "Summary" -- headings do NOT contain "databaseconnection"
      or "pool", so the guard will not suppress it)
    - /home/houminxi/code/plan-forge/tests/fixtures/f2_heading_fp.md
      (Phase 8 FP fixture; pattern reference for new fixtures)
    - /home/houminxi/code/plan-forge/src/plan_forge/checks/mechanical/f2_duplicate_fact.py
      (full file, 83 lines; understand _CAP_TOKEN_RE capture rules and the
      heading_words guard at lines 61-68)
    - /home/houminxi/code/plan-forge/tests/unit/test_f2_duplicate_fact.py
      (full file, 79 lines; add new functions after test_f2_genuine_phrase_not_suppressed)
  </read_first>

  <behavior>
    - test_f2_structural_fp_suppressed: asserts findings == []. Against unmodified
      source (only heading_words guard, no structural-word guard), structural
      n-grams like "run expected", "expected pass" fire F2 -- test FAILS. RED
      gate for F2-01.
    - test_f2_signoff_fp_suppressed: asserts findings == []. Against unmodified
      source (no signoff extraction), author-name n-grams like "minxi hou" fire
      F2 -- test FAILS. RED gate for F2-02.
  </behavior>

  <acceptance_criteria>
    - tests/fixtures/f2_structural_fp.md exists, has 4 sections each containing
      "Run ... Expected ... Pass" pattern with capitalized tokens
    - tests/fixtures/f2_signoff_fp.md exists, has 3 sections each containing
      "Signed-off-by: Minxi Hou <houminxi@gmail.com>"
    - test_f2_structural_fp_suppressed FAILS against unmodified source
    - test_f2_signoff_fp_suppressed FAILS against unmodified source
    - All 6 pre-existing F2 tests still pass
    - Step 0 clean on all modified/new files
  </acceptance_criteria>

  <action>
    All edits inside .worktrees/f2-structural-fp/.

    FIXTURE f2_structural_fp.md: Four sections, each containing capitalized
    tokens "Run", "Expected", "Pass" so _extract_ngrams generates n-grams
    like "run expected", "expected pass", "run expected pass" appearing in
    4 distinct sections (>= 3 threshold). Section headings ("Task A",
    "Task B", etc.) must NOT contain "run", "expected", or "pass" as heading
    words -- otherwise the existing heading_words guard would suppress them
    and the RED state would not fire.

    Exact content:

      ## Task A

      Step 1: Write the module.
      Run tests.
      Expected: Pass.

      ## Task B

      Step 2: Create the config.
      Run linter.
      Expected: Pass.

      ## Task C

      Step 3: Modify the schema.
      Run migration.
      Expected: Pass.

      ## Task D

      Step 4: Add tests.
      Run coverage.
      Expected: Pass.

    FIXTURE f2_signoff_fp.md: Three sections, each containing a Signed-off-by
    line with "Minxi Hou". _CAP_TOKEN_RE matches "Minxi" (5 chars, starts
    uppercase) and "Hou" (3 chars, starts uppercase). _extract_ngrams produces
    the bigram "minxi hou" in 3 distinct sections. "Signed-off-by" itself
    starts lowercase so it does NOT match _CAP_TOKEN_RE and does not appear
    in n-grams directly. Section headings ("Task A", etc.) do not contain
    "minxi" or "hou", so the heading_words guard does not suppress them.

    Exact content:

      ## Task A

      Do this task.

      Signed-off-by: Minxi Hou <houminxi@gmail.com>

      ## Task B

      Do that task.

      Signed-off-by: Minxi Hou <houminxi@gmail.com>

      ## Task C

      Do another task.

      Signed-off-by: Minxi Hou <houminxi@gmail.com>

    TEST STUBS: Add two functions after test_f2_genuine_phrase_not_suppressed
    (end of test_f2_duplicate_fact.py):

      def test_f2_structural_fp_suppressed():
          """Structural template phrases must not trigger F2."""
          parsed = parse(_load("f2_structural_fp.md"))
          findings = f2_duplicate_fact.check(parsed)
          assert findings == [], (
              f"Expected structural phrases suppressed, got: {findings}"
          )


      def test_f2_signoff_fp_suppressed():
          """Signed-off-by author names must not trigger F2."""
          parsed = parse(_load("f2_signoff_fp.md"))
          findings = f2_duplicate_fact.check(parsed)
          assert findings == [], (
              f"Expected signoff names suppressed, got: {findings}"
          )

    Before adding stubs, verify RED state by temporarily creating the fixture
    and running a quick inline check to confirm at least one n-gram fires
    against unmodified source. If either fixture produces zero findings against
    unmodified source (fixture is invalid), revise the fixture so the target
    n-grams appear in >= 3 distinct section bodies.

    Step 0 pre-check on all new/modified files (run from worktree root):
      python3 -m py_compile tests/unit/test_f2_duplicate_fact.py
      ruff check tests/unit/test_f2_duplicate_fact.py
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -P '[^\x00-\x7F]' && echo "FAIL: non-ASCII" || echo "OK"
      git diff --diff-filter=AM -U0 | grep '^+' |
        grep -iE 'T[0-9]{1,2}[: ]|D[0-9]+\b|SC-[0-9]|P[0-9]+:|S[0-9]+:' &&
        echo "FAIL: AI smell" || echo "OK"

    Confirm RED: run both new tests in isolation -- both must FAIL.
      uv run pytest tests/unit/test_f2_duplicate_fact.py::test_f2_structural_fp_suppressed -xvs
      uv run pytest tests/unit/test_f2_duplicate_fact.py::test_f2_signoff_fp_suppressed -xvs

    Then run the full F2 test file to confirm pre-existing tests still pass:
      uv run pytest tests/unit/test_f2_duplicate_fact.py -q
    Expected: 6 passed, 2 failed.
  </action>

  <verify>
    <automated>
      cd /home/houminxi/code/plan-forge/.worktrees/f2-structural-fp && uv run pytest
      tests/unit/test_f2_duplicate_fact.py -q 2>&1 | tail -10
    </automated>
  </verify>

  <done>
    Two fixture files created. test_f2_structural_fp_suppressed and
    test_f2_signoff_fp_suppressed both FAIL against unmodified source (RED
    confirmed). All 6 pre-existing F2 tests still pass. Step 0 clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture markdown -> parser | Fixed-content .md files; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SC | Tampering | no new packages installed | accept | no package manager installs in this plan; legitimacy gate not applicable |
</threat_model>

<verification>
From inside .worktrees/f2-structural-fp/:

  uv run pytest tests/unit/test_f2_duplicate_fact.py -q

Expected: 6 passed, 2 failed -- test_f2_structural_fp_suppressed and
test_f2_signoff_fp_suppressed in FAILED state. All 6 pre-existing F2 tests
pass. Note: 1 pre-existing failure (test_version_is_alpha2) is unrelated to
this phase.
</verification>

<success_criteria>
1. git worktree list shows .worktrees/f2-structural-fp at fix/f2-structural-fp-v012
2. tests/fixtures/f2_structural_fp.md and f2_signoff_fp.md exist
3. test_f2_structural_fp_suppressed: FAILED (findings != [])
4. test_f2_signoff_fp_suppressed: FAILED (findings != [])
5. All 6 pre-existing F2 tests: PASSED
6. Step 0 clean on modified test file
7. Main worktree git status: clean (no modifications)
</success_criteria>

<output>
Create .planning/phases/10-f2-structural-false-positive-reduction/10-01-SUMMARY.md when done.
</output>
